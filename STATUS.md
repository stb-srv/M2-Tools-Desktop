# M2Manager – Status & Roadmap

Diese Datei ist die laufende Referenz für „was ist schon gebaut" vs. „was fehlt noch". Wird bei jeder größeren Session aktualisiert/abgehakt. Quelle der Ursprungs-Features: `Idee.md` + `weitere Informationen.md`.

Stand: 2026-08-08

---

## ✅ Fertig / funktionsfähig

- [x] **Projekt-Grundgerüst** — Tauri v2 + React/TS/Vite, Tailwind + shadcn-artige UI-Komponenten, Zustand-Store
- [x] **Dark/Light Theme** — `src/store/theme.ts`, `prefers-color-scheme`-Erkennung + manueller Override
- [x] **i18n-Grundgerüst** — react-i18next eingebunden, lädt bereits **Deutsch + Englisch** (`src/i18n/locales/{de,en}.json`)
- [x] **Connection Manager** — SSH- und MySQL-Profile inkl. „Verbindung testen", Secrets über Windows Credential Manager (`keyring`, `windows-native`-Feature — siehe [[m2manager_credentials_bug]]). **2026-07-29: eigener „Verbindungen"-Tab entfernt, Inhalt in die Einstellungen verschoben** — die Einstellungsseite ist jetzt in die Kategorien „Allgemein" / „Server" (SSH, MySQL, Mysql2Proto, VNUM-Bereich, Mob-Drop-Dateipfad) / „Client" (Client-Pfad, NPC-Liste, EterPackConsoleLz4) sortiert
- [x] **SSH-Backend** — `ssh.rs`, inkl. Fallback auf keyboard-interactive Auth (siehe [[m2manager_ssh_auth]])
- [x] **Path Configuration + Setup Wizard** — geführter Erststart (Pfade → SSH → MySQL), `SetupWizard.tsx`
- [x] **Server-Steuerung** — Start/Stop/Logs löschen/Quests reloaden über die interaktive `index.sh`, Live-Log-Streaming per Tauri-Events, Bestätigungsdialog bei destruktiven Aktionen. **Neustarten-Button** (2026-07-29): führt Stop- und Start-Befehl nacheinander aus mit konfigurierbarer Wartezeit dazwischen (Standard 5s, editierbar), eigener Bestätigungsdialog, keine neuen Backend-Commands nötig (nutzt `run_server_command` zweimal) (siehe [[m2manager_server_control]])
- [x] **DB-Schema-Mapping** — `db/mapping.rs`, verifiziertes Standard-Mapping gegen echten Dev-Server (siehe [[m2manager_db_schema]])
- [x] **Generischer Database Explorer** — freie Tabellenansicht für beliebige Tabellen (`DbExplorer.tsx`, `db/explorer.rs`)
- [x] **Shop Editor** — Grid-UI wie im Client, NPC-Auswahl mit 3D-Preview, Item-Suche/-Filter, Mengen-Steuerung, 40-Slot-Limit, Lösch-Bestätigung (siehe [[m2manager_item_icons]])
- [x] **Item-Icons** — Lookup aus `icon.epk`-Extract inkl. Decade-Fallback für Refine-Ketten
- [x] **3D Model Viewer (GR2)** — 32-Bit-Sidecar lädt Client-eigene `granny2.dll`, Rigid- **und** Skinned-Mesh-Extraktion, three.js-Rendering (`Gr2Canvas.tsx`), NPC-Textur-Anwendung (`textures.rs`) (siehe [[m2manager_gr2_reference]])
- [x] **Dashboard** — erste Version vorhanden (`Dashboard.tsx`)
- [x] **Lokale Settings-DB** — SQLite via `rusqlite`, `get_setting`/`set_setting`
- [x] **Item Editor v1 (ohne 3D-Modell)** — neue Items anlegen **und bestehende bearbeiten**: DB-Insert/-Update in `player.item_proto` (`db/item.rs`), Icon-Schreiben nach `pack/icon/icon/item/<vnum>.tga` (`packtools.rs`, beim Bearbeiten optional), automatisches Neu-Packen von `icon.epk` und Neuerzeugen/Einspielen von `item_proto` über die externen Tools `EterPackConsoleLz4.exe`/`Mysql2Proto.exe` (beide per CLI angesteuert, Backups vor jedem Überschreiben, Bestätigungsdialog vor der destruktiven Kette, Vorab-Prüfung der Tool-Pfade vor jedem DB-Write, automatischer Rollback bei Fehlschlag). Waffen/Rüstungen mit **neuem** 3D-Modell sind bewusst noch nicht Teil davon (v2) — siehe Plan `dapper-snuggling-cat.md` und [[m2manager_item_editor]]
- [x] **Mob Drop Editor** — lädt/bearbeitet/speichert `mob_drop_item.txt` direkt auf dem Server per SFTP (erste echte SFTP-Nutzung im Projekt, `ssh.rs::read_remote_file`/`write_remote_file_with_backup`, `russh-sftp` war seit Projektstart ungenutzte Dependency). Strikter Parser/Serializer (`mobdrop.rs`, parse-or-refuse, immer lückenlose Neu-Nummerierung der Drop-Einträge beim Speichern), Item-Auswahl wie im Shop Editor (Suche + Icons), vier Bulk-Änderungsmodi für Drop-Prozente (Delta, Fest, Zufallsbereich, bestimmtes Item überall) je mit Global/Nur-dieser-Mob-Scope, serverseitiges Backup vor jedem Überschreiben. Zeigt zusätzlich "≈X% real" pro Prozentfeld (aus dem echten Server-Quellcode verifizierte Formel: reale Chance ≈ Prozentwert ÷ 4 — **2026-08-03 korrigiert**: eine frühere Version hatte das ÷400-Ergebnis als Bruch statt als Prozentzahl behandelt und dadurch 100× zu niedrige Werte gezeigt, aufgefallen durch reale Spieltest-Beobachtung eines Nutzers, gegen `item_manager.cpp`/`item_manager_read_tables.cpp` nachgerechnet und in `dropChance.ts` mit Regressionstest fixiert) über ein "i"-Info-Popover ("Serverfiles von Fliege V3 - Fratello", erweiterbare Datenliste in `serverNotes.ts`). Zweiter Tab **"Lokale Datei prüfen/reparieren"**: wählt eine beliebige lokale `mob_drop_item.txt`-Kopie, validiert sie mit demselben strikten Parser, und bei Syntaxfehlern gibt es eine Rohtext-Recovery-Ansicht (Fehlermeldung mit Zeilennummer + Textarea + "Erneut prüfen") bis die Datei sauber einliest — danach im selben strukturierten Editor weiterbearbeitbar, Speichern schreibt lokal mit Backup — siehe [[m2manager_mob_drop_editor]]
- [x] **MySQL-Auto-Reconnect beim App-Start** — `src/lib/mysqlConnect.ts` verbindet automatisch mit den gespeicherten Zugangsdaten, bevor die UI überhaupt gerendert wird; vorher musste man nach jedem Neustart einmal in den Connections speichern, damit z.B. Shop Editor/Item Editor/Dashboard funktionierten (siehe [[m2manager_item_editor]])
- [x] **TGA Konverter** — eigener Tab, wandelt PNG/JPG/BMP/GIF beliebig gewählter Dateien in `.tga` um (Mehrfachauswahl, Vorschau, editierbarer Zielpfad pro Datei, "Zielordner für alle setzen"). Teilt sich die Konvertierungslogik (`imageconv.rs`) mit dem Item Editor — **die Icon-Konvertierung beim Item-Anlegen/-Bearbeiten lief technisch schon vorher automatisch**, war aber auf PNG/TGA als Eingabe beschränkt (Cargo-Feature-Flags); jetzt zusätzlich JPG/BMP/GIF, was beiden Stellen zugutekommt
- [x] **Regen-Datei-Editor** (2026-07-30) — bearbeitet die Monster-Spawn-Dateien, auf die Dungeon-Etagen im Quest Builder verweisen (`share/data/dungeon/**/*_regen.txt`). Format 1:1 aus dem Server-Quellcode (`regen.cpp::read_line`) verifiziert, nicht angenommen: `<typ> x y radius_x radius_y z_sektion richtung zeit prozent max_anzahl vnum`, Sonderfall `e` (Ausnahmezone) bricht nach 5 Werten ab. SFTP-Ordnerbrowser (neu: `ssh::list_remote_dir`, `russh_sftp::read_dir` war bisher ungenutzt) + strukturierter Editor pro Zeile (Spawn/Ausnahmezone/unverändert übernommene Kommentar- oder Leerzeilen), parse-or-refuse wie beim Mob Drop Editor, Backup vor jedem Speichern. Neue Einstellung `regen_base_dir` (siehe [[m2manager_quest_builder]])
- [x] **Locale-String-Verwaltung** (2026-07-30) — bearbeitet `share/translate.lua` (~8860 Zeilen), die Textbausteine, die Quests über `gameforge.questname._key` referenzieren (Format aus der Datei selbst dokumentiert, dort als offizielle "Gameforge-Way-To-Do"-Konvention beschrieben). Bewusst kein Voll-Parse/Reserialize der ganzen Datei — `write_namespace` ersetzt nur die Zeilen des bearbeiteten Namespace anhand von Präfix-Matching, der Rest von ~8800 nicht im Detail modellierten Zeilen bleibt byte-identisch erhalten. Neue/bestehende Namespaces, Keys hinzufügen/entfernen/bearbeiten, `[ENTER]`-Marker wird wie im Quest Builder als echter Zeilenumbruch in der Textarea angezeigt. Neue Einstellung `locale_file_path` (siehe [[m2manager_quest_builder]])
- [x] **Backup-Browser** (2026-07-30) — durchsucht die verstreuten `m2manager_backups`-Ordner (jeder Editor legt sein Backup direkt neben der Originaldatei an, kein zentraler Ort) und erlaubt Wiederherstellen einer Backup-Datei an ihren Ursprungsort — die aktuell dort liegende Datei wird dabei selbst zuerst gesichert statt einfach überschrieben. Nutzt denselben neuen `list_remote_dir`-Befehl wie der Regen-Datei-Editor
- [x] **Quest-Funktionsreferenz im Code-Editor** (2026-07-30) — durchsuchbares Seitenpanel mit allen 611 verifizierten Quest-Lua-Funktionen (`share/quest/quest_functions` vom echten Server), Klick fügt die Funktion an der Cursor-Position im Rohcode-Editor ein
- [x] **Bugfix: Namenssuche in Item-/Mob-Suche fand nur exakte Groß-/Kleinschreibung, Database Explorer ebenfalls behoben** (2026-07-30) — derselbe Fix wie unten bei Quest Builder beschrieben, zusätzlich auch in `db/explorer.rs::search_rows` (generischer Database-Explorer-Suche) angewendet, dort nur wenn die Spalte tatsächlich `binary`/`varbinary` ist
- [x] **Gemeinsamer Fehler-Helfer gegen stillschweigend verschluckte Fehler** (2026-07-30) — `src/lib/asyncAction.ts`: `runAsyncAction` macht `onError` zur Pflichtangabe, damit ein `try {...} finally {...}` ohne `catch` (der exakte Bug unten bei Mob Drop Editor/Quest Builder) strukturell nicht mehr passieren kann; an beiden Stellen bereits eingesetzt, mit eigenen Vitest-Tests abgesichert
- [x] **Frontend-Tests eingerichtet (Vitest)** (2026-07-30) — bisher gab es nur Rust-Tests; jetzt 24 Vitest-Tests für die heikelsten TS-Stellen: alle 6 Quest-Builder-Vorlagen-Generatoren (inkl. strukturellem `begin`/`if`/`end`-Balance-Check), die verifizierten Waffen-Tooltip-Formeln aus dem Item Editor, `asyncAction.ts`. `npm run test` bzw. `npx vitest run`
- [x] **Quest Builder** (2026-07-29) — verwaltet die echten Quest-Quelldateien auf dem Server per SFTP: Layout (`share/quest/<Kategorie>/<Name>.lua`, Liste in `quest_list`, Kompilierung über das vorhandene `qc_x64`/`make.py`) direkt auf dem Dev-Server verifiziert, nicht angenommen. Links Kategorie-/Datei-Browser (aus `quest_list` geparst), rechts Rohcode-Editor mit Speichern (SFTP-Backup vor jedem Überschreiben) und Löschen (kein Hard-Delete, Datei wandert ins Backup-Verzeichnis, Eintrag aus `quest_list` entfernt). "+"-Dialog erzeugt neue Quests aus 4 Vorlagen (NPC-Dialog+Belohnung, Sammel-Quest, Kill-Quest, Item-Benutzung) mit Item-/Mob-Auswahl (Suche wie im Shop/Mob-Drop-Editor, neuer `search_mobs`-Command); die generierte Lua-DSL-Syntax (`when NPC.chat."Label" [with Bedingung] begin…end`, `when MOB.kill`, `when ITEM.use`, `pc.give_item2`, `pc.getqf/setqf` usw.) stammt aus echten Quest-Dateien auf dem Server (`Pferd_rufen.lua`, `Orkzahn.lua`), nicht aus generischer Metin2-Doku. "Kompilieren & Neuladen"-Button nutzt denselben Befehl wie "Quests reloaden" in der Server-Steuerung und zeigt die Compiler-Ausgabe live. Vorlagen decken bewusst nur einfache Ein-Zustand-Quests ab — komplexe Multi-State-Quests bleiben Sache des Rohcode-Editors. **2026-07-30: 5. Vorlage "Run / Instanz-Dungeon"** ergänzt, nachdem der Nutzer einen "Runs Ersteller" anhand der 5 echten `.quest`-Dateien im `Runs`-Ordner wünschte: alle 5 echten Runs direkt vom Server gelesen (per SFTP-Diagnose) und analysiert — 3 davon (Beran_Eintritt, Daemonenturm, Teufelskatakomben) sind hochgradig individuelle Boss-Mechaniken (Passwort-Systeme, Timer-Ketten, Gruppen-Leader-Tracking) und bewusst **nicht** 1:1 nachbaubar; das gemeinsame Grundmuster aller Runs (mehrstufige Instanz: NPC-Einstieg → `d.regen_file`/`d.setf("level",N)` pro Etage → Boss-Kill schaltet nächste Etage frei via `d.jump_all`/`d.clear_regen` → letzte Etage vergibt Belohnung und wirft über `server_timer`+`d.exit_all()` raus) ist dagegen verifiziert und als Vorlage abgebildet — mit dynamischer Etagen-Liste (Boss-VNUM, Einstiegskoordinaten, Regen-Datei-Pfad pro Etage). Zwei Dinge muss der Nutzer selbst liefern (nicht aus den Quest-Dateien ableitbar): den Dungeon-Map-Index-Bereich (aus der Server-Dungeon-Konfiguration) und die Regen-Dateien selbst (eigenes Format, wird hier nicht erzeugt). Backend-Fix nebenbei: `create_quest_file` erlaubt jetzt eine wählbare Dateiendung (Runs nutzen `.quest`, normale Quests weiter `.lua`). **2026-07-30, Bugfixes nach erstem Nutzertest:** (1) Manche Quest-Dateien sind nicht UTF-8 sondern Windows-1252 (z.B. `Biologie/Biochecker.lua`) — Datei-Lesen brach bisher hart ab, liest jetzt mit UTF-8-zuerst-dann-Windows-1252-Fallback (betrifft auch Mob Drop Editor). (2) NPC/Mob-Namenssuche fand nur bei exakter Groß-/Kleinschreibung Treffer, weil `locale_name` eine `varbinary`-Spalte ohne Kollation ist (reiner Byte-Vergleich) — behoben über `LOWER(CONVERT(... USING latin1))` auf beiden Seiten, live gegen die echte DB verifiziert (inkl. Umlaute); betrifft auch Shop Editor und Mob Drop Editor, da alle dieselbe `search_items`/`search_mobs`-Funktion nutzen. (3) Neue 6. Vorlage **"Sammel-Quest mit Erfolgschance (Biologie-Stil)"** — Item wird bei jedem Versuch verbraucht, zählt aber nur mit einstellbarer Prozent-Chance zum Fortschritt (Muster 1:1 aus `Biologie/Orkzahn.lua` übernommen). **Noch nicht erneut live getestet** (siehe [[m2manager_quest_builder]])
- [x] **Vier Verbesserungen an bestehenden Editoren** (2026-08-03, aus einer Ideen-Session heraus umgesetzt, siehe `new_features.md` für die Ideensammlung selbst):
  - **Item Editor**: „Als neues Item duplizieren"-Button im Bearbeiten-Modus — holt direkt die nächste freie vnum und wechselt mit den aktuellen Feldwerten vorbefüllt in den Create-Modus (Icon muss weiterhin neu gewählt werden). Vorher musste man dafür Typ/Flags/Werte über „Referenz-Item übernehmen" manuell erneut suchen.
  - **Backup-Browser**: „Diff anzeigen"-Button vor dem Wiederherstellen zeigt einen Zeilen-Diff (via `diff`-npm-Paket) zwischen Backup-Inhalt und aktuellem Stand der Zieldatei, inkl. Fall „Zieldatei existiert aktuell nicht mehr". Backend: `diff_remote_backup`-Command, Zielpfad-Ableitung aus `restore_remote_backup` nach `backups::target_path_for_backup` extrahiert (von beiden geteilt, kann jetzt nicht mehr auseinanderlaufen).
  - **Quest Builder: Volltextsuche** — durchsucht (per Enter/Button) den *Inhalt* aller Quest-Dateien, nicht nur Datei-/Kategorienamen wie die bisherige Live-Filterung. Neue `ssh::read_remote_files`, die eine SFTP-Session für mehrere Dateien wiederverwendet (die bisherigen `read_remote_file`-Aufrufe öffnen pro Datei eine neue SSH-Verbindung — für eine ganze Quest-Kartei viel zu langsam gewesen). Klick auf einen Treffer öffnet die Datei und springt zur Zeile.
  - **Quest Builder: Lua-Syntax-Highlighting** — Rohcode-Editor von einer reinen `<textarea>` auf CodeMirror (`@uiw/react-codemirror` + `@codemirror/legacy-modes`-Lua-Mode) umgestellt, folgt Hell/Dunkel automatisch (MutationObserver auf die `dark`-Klasse an `<html>`, da der Theme-Store das nicht direkt reaktiv exponiert). Cursor-Insert der Funktionsreferenz läuft jetzt über die CodeMirror-`view.dispatch`-API statt DOM-`selectionStart`.
- [x] **Alle 8 Ideen aus `new_features.md` umgesetzt** (2026-08-03):
  - **Mob-Proto-Editor** (`src/features/mob-proto-editor`) und **Account-/Spieler-Verwaltung** (`src/features/account-manager`) — beide auf einer neuen generischen DB-Zeilen-CRUD-Grundlage aufgebaut (`db/explorer.rs::get_row_by_pk`/`update_row`/`insert_row`/`delete_row`, Commands `get_table_row`/`update_table_row`/`insert_table_row`/`delete_table_row`), da `mob_proto`/`account`/`player` anders als `item_proto` nie live gegen einen echten Server verifiziert wurden. Statt Spaltenbedeutung zu raten, zeigt der geteilte `GenericRowEditor` (`src/features/shared/GenericRowEditor.tsx`) jede Spalte roh mit typgerechtem Eingabefeld und erkennt die Primärschlüssel-Spalte automatisch über `information_schema` (`is_primary_key`) statt "id"/"vnum" anzunehmen. Binary/varbinary-Spalten werden beim Schreiben symmetrisch zur bestehenden Lesekodierung Windows-1252-kodiert (schließt denselben Bug-Typ wie bei `locale_name` von vornherein aus). Mob-Proto-Editor: nur Bearbeiten, kein Neuanlegen (Mysql2Proto-Äquivalent für mob_proto nicht verifiziert), Hinweis dass Änderungen i.d.R. erst nach Server-Neustart wirken. Account-Manager: Suche über `account.account`/`player.player` (Suchspalte editierbar, Default `login`/`name`), generischer Insert/Delete auf `player.item` für "Item geben/entfernen" (Fenster/Position-Semantik bewusst nicht geraten, Nutzer füllt Spalten selbst). Kein Kanal zu einem laufenden Spielprozess vorhanden - "Teleport" wirkt bei Online-Spielern ggf. erst nach Neuanmeldung.
  - **Reverse-Lookup für Drops** — neuer "Wer droppt…?"-Button im Mob Drop Editor, rein clientseitig (die Datei ist beim Öffnen schon komplett geladen), kein neuer Backend-Command nötig.
  - **Server-Ressourcen-Monitoring** (`src-tauri/src/resources.rs`) — liest `ps -axo pid,pcpu,pmem,rss,comm` über die bestehende SSH-Verbindung (bewusst kein Linux-`/proc`, da dieser Dev-Server FreeBSD ist), gefiltert nach der neuen Einstellung `server_process_names` (Default `game,db`, nie gegen einen echten Server verifiziert welche Prozessnamen dieser Core nutzt). Im Dashboard als Tabelle sichtbar, sobald SSH verbunden ist. **2026-08-03 erweitert:** neue "Server-Übersicht"-Sektion im Dashboard zeigt zusätzlich die konfigurierte SSH-IP, RAM (`sysctl hw.physmem`/`vm.stats.vm.v_free_count`, FreeBSD-Standard-Sysctls, "frei" zählt bewusst nur echte freie Seiten ohne Cache) und Festplattenbelegung (`df -Pk` auf einem konfigurierbaren Pfad, neue Einstellung `server_disk_path`) — ein einziger kombinierter SSH-Befehl (`get_server_overview`) statt zwei getrennter Verbindungen.
  - **Automatisierte Datenbank-Backups** (`src/features/db-backups`, `src-tauri/src/db_backup.rs`) — `mysqldump`/`mysql`-Restore über SSH mit den bereits konfigurierten MySQL-Zugangsdaten (Passwort via `MYSQL_PWD`, nicht als CLI-Arg sichtbar), neue Einstellungen `db_backup_dir`/`db_backup_databases`. Erstellen/Auflisten/Wiederherstellen/endgültiges Löschen, mit deutlicher Warnung vor dem Restore (überschreibt die komplette Live-DB). **Noch nie live gegen einen echten Server getestet** (wie schon beim Mob Drop Editor SFTP).
  - **Discord-/Webhook-Benachrichtigungen** (`src-tauri/src/webhook.rs`, neue Einstellung `webhook_url`) — generischer `notify_webhook_message`-Command, verdrahtet in: fehlgeschlagene DB-Backups (direkt im Rust-Command) und einen neuen `CrashWatch`-Komponente (`src/components/CrashWatch.tsx`, an der App-Wurzel gemountet), die alle 60s die Server-Ressourcen-Prozessliste prüft und bei Übergang "lief" → "läuft nicht mehr" benachrichtigt. Bewusst kein echter Server-seitiger Watchdog - läuft nur solange die App offen ist.
  - **Command Palette (Strg+K)** (`src/components/CommandPalette.tsx`) — springt zu jedem Sidebar-Bereich, Liste geteilt mit der Sidebar über eine neue gemeinsame `NAV_ITEMS`-Quelle in `store/navigation.ts` (vorher zwei getrennte Listen in Sidebar.tsx).
  - **Icon-/Textur-Browser** (`src/features/icon-browser`) — durchsucht rekursiv alle `.tga` unter `pack/icon/icon/item` im Client-Ordner (neue `icons::list_item_icon_files`/`load_icon_file_data_url`, Thumbnails lazy pro sichtbarer Kachel geladen, mit Pagination), sowohl als eigener Sidebar-Bereich als auch als Auswahl-Modal im Item Editor ("Aus Icon-Browser wählen…").
  - Alle neuen Bereiche in `NAV_ITEMS`/`App.tsx`/i18n (de/en) verdrahtet. `new_features.md` als erledigt markiert.
- [x] **Bugfix: Item Editor zeigte beim Bearbeiten leere/falsche Werte** — echter Root-Cause gefunden (nicht geraten): mehrere `item_proto`-Spalten sind auf diesem Server SIGNED (`type`/`subtype`/`weight`/`size`/`magic_pct`/`limittype0-1`/`applytype0-3`/`specular`/`socket_pct` als signed tinyint, `antiflag`/`flag`/`wearflag`/`gold` als signed int), aber die Rust-Structs erwarteten unsigned — sqlx lehnt das beim Lesen ab, und `.unwrap_or_default()` hat den Fehler bisher lautlos verschluckt (0 statt Fehler). Per `information_schema.columns`-Abfrage gegen die echte Dev-DB verifiziert und alle Typen korrigiert. Zusätzlich: `immuneflag` ist gar kein Bitmask-Integer, sondern ein MySQL `SET('PARA','CURSE','STUN','SLEEP','SLOW','POISON','TERROR')` — eigene Konvertierungs-Helfer geschrieben, `IMMUNE_FLAGS` im Frontend auf die echten SET-Namen korrigiert. Permanenter Regressionstest gegen vnum 3219 auf der echten DB grün (siehe [[m2manager_item_editor]])
- [x] **Modul-Importer** (`src/features/module-importer`, 2026-08-06) — automatischer Import fertiger Waffen-/Rüstungs-Pakete (beliebige Ordnerstruktur, generischer Scan statt fixem Layout). Waffen: Subtyp-Erkennung per Dateinamens-Alias, Icon-Fuzzy-Matching, Modell wird nach `{vnum}.gr2` umbenannt (Textur behält Originalnamen). Rüstung: automatische 3D-Modell-Verknüpfung für weibliche Charaktere über die Client-`.msm`-Datei (`ShapeData`), männlich bleibt manuell (verifiziert: dafür gibt es clientseitig keine Datei-basierte Zuordnung). Import-Verlauf mit vollständigem Rückgängig-machen. Mehrere reale Bugs dabei gefunden und behoben: RLE-komprimierte TGA-Icons wurden vom Client nicht gelesen (unsichtbares Icon), Waffen mit absolutem (statt relativem) Textur-Pfad im Modell blieben weiß im Client (Fix kopiert die Textur zusätzlich an den exakten erwarteten virtuellen Pfad), und die Pack-Tool-Ausgabe wurde fälschlich als UTF-8 statt Windows-1252 gelesen (brach bei jedem Umlaut im `item`-Ordner ab und nahm dabei den ganzen Import zurück) (siehe [[m2manager_module_importer]])
- [x] **Zwei reale Bugs gefunden und behoben (2026-08-10)**: (1) Aufwertungs-Ketten-Namensgebung kollidierte ab Stufe +10 - `fitToByteLimit(base + suffix)` schnitt bei knappem Basisnamen einfach das letzte Zeichen der GESAMTEN Zeichenkette ab, was immer die letzte Ziffer der Stufenzahl traf ("+10"/"+11"/"+12"... wurden alle zu "+1"). Neues `fitToByteLimitWithSuffix` reserviert das Suffix zuerst, kürzt nur noch die Basis - Regressionstest ergänzt (`ModuleImporter.test.ts`). (2) **Deutlich schwerwiegenderer Fund bei der Recherche dazu**: "Rückgängig machen" hatte das komplette `icon.epk` des Clients gelöscht (Nutzer musste aus einem Client-Backup wiederherstellen). Live in einer isolierten Sandbox verifiziert (echtes `EterPackConsoleLz4.exe` + echte `icon.epk`-Kopie, nie das echte Client-Verzeichnis angefasst): das Pack-Tool **ersetzt das komplette Archiv** durch nur das, was gerade lose im Ordner liegt, statt es zu mergen - ein Test-Repack mit nur 1 loser Datei hat ein echtes 13,6MB/~3500-Icons-`icon.epk` auf 2KB/1 Icon geschrumpft. Da diese App immer nur die selbst angefassten Icons/Modelle lose schreibt (nie den kompletten Client-Bestand), war **jeder** Repack-Aufruf in der App (nicht nur Rückgängig-machen) latent betroffen. Behoben in `packtools::run_eterpack_pack` (schützt alle Aufrufer auf einmal): Backup von `.epk`/`.eix` vor jedem Repack (bisher als einzige Stelle im ganzen Projekt ohne das übliche Backup-vorher-Muster), plus ein Merge-Schritt, der das aktuelle Archiv in eine isolierte Kopie entpackt und nur fehlende Dateien lose nachträgt (nie eine bereits lose vorhandene Datei überschreibt - Unit-getestet). Nicht rückwirkend behoben: der wiederhergestellte Client-Stand des Nutzers spiegelt evtl. nicht jeden vor der Wiederherstellung über diese App angelegten Custom-Item wider (siehe [[m2manager_module_importer]])
- [x] **Aufwertungs-Editor / Refine-Editor** (`src/features/refine-editor`, `src-tauri/src/refine.rs`, 2026-08-06) — visualisiert die komplette Aufwertungs-Kette eines Items (alle Stufen, Materialien, Gold-Kosten, Erfolgschance) statt roher `refine_proto`-Zeilen, inkl. Bearbeiten/Anlegen/Wiederverwenden von Rezepten und „Woher bekomme ich das?"-Hinweisen (Mob-Drops + NPC-Shops) pro Material. Mechanik im echten Server-Quellcode verifiziert, nicht angenommen: `item_proto.refine_set` ist eine reine Rezept-ID (`refine_proto.id`), hat nichts mit einer Item-Vnum zu tun und wird oft von mehreren Items geteilt (Rezept 1 z.B. von 8 Items); `refine_proto.src_vnum`/`result_vnum` existieren in der Tabelle, werden vom Server aber nie gelesen (Karteileichen). **Wichtig:** anders als alle anderen Editoren gibt es keinen Client-Repack — der DB-Prozess lädt `refine_proto` nur beim Start, Änderungen wirken erst nach Server-Neustart (Hinweis + Link zur Server-Steuerung im UI). Noch nicht live im Client durchgetestet, nur gegen die echte Dev-DB (Regressionstests gegen reale Ketten wie vnum 180 und den Schwert-10-19-Chain)
- [x] **Modul-Importer: automatische Aufwertungs-Kette** (2026-08-06) — importierte Waffen/Rüstungen können jetzt optional direkt eine komplette +0-bis-+N-Kette bekommen statt einer einzelnen Stufe, inkl. verknüpfter `refine_proto`-Rezepte (Gold-Kosten/Chance nach echter Stock-Vorlage dieses Servers, keine Materialien — lassen sich danach im Aufwertungs-Editor ergänzen). Icon/3D-Modell werden nur einmal geschrieben und über die ganze Kette geteilt (Stock-Konvention). Werte-Wachstum linear zur Basis, konfigurierbarer Prozentsatz, betrifft bei Waffen Schaden min/max + Zusatz-Angriffskraft, bei Rüstung Magie-/Verteidigung + Zusatz-Verteidigung (nicht `value3` — das ist bei Rüstung der 3D-Modell-Shape-Index, bleibt live-verifiziert über echte Stock-Ketten konstant). Rückgängig-machen/Rollback räumt jetzt auch nicht mehr benötigte `refine_proto`-Zeilen mit auf (nur wenn wirklich kein anderes Item sie noch nutzt)
- [x] **Modul-Importer: Icon-Item-Modus (kein 3D-Modell)** (2026-08-06) — zweiter Modus im selben Bereich, für Zubehör ohne eigenes Modell (Schuhe, Ketten, Schilder, Ohrringe, Armbänder, Verbrauchsgegenstände, …). Ordner voller loser Bilddateien wählen, pro Bild ein Item mit frei wählbarem Typ (beliebiger `item_proto.type`, Dropdown zeigt „Label (Code)“), Subtyp, Trageort, Basiswerten (value0-5, optional von einem Referenz-Item übernommen) und **pro Wert** einer „wächst“-Checkbox — bewusst manueller als der Waffen-/Rüstungs-Import, da value0-5 je nach Typ/Subtyp etwas anderes bedeutet und es keinen sinnvollen generischen Standard gibt. Jedes Item bekommt optional seine eigene Aufwertungs-Kette (eigene Maximalstufe/Wachstum), nutzt intern dieselben Bausteine wie der Waffen-/Rüstungs-Import
- [x] **Kisten-Editor** (`src/features/box-editor`, `src-tauri/src/special_item_group.rs`, 2026-08-06) — bearbeitet `special_item_group.txt`, die Beute-Tabelle einer GIFTBOX (Kiste). Wichtiger Fund: die „verbleibende Anzahl“ beim mehrfachen Öffnen einer Kiste ist keine Extra-Funktion, sondern schlicht die Stapelanzahl des Items (`ITEM_GIFTBOX` im Server-Quellcode zieht bei jedem Öffnen 1 vom Stapel ab) — keine Server-Änderung nötig, nur Item-Typ „GIFTBOX“ (23) + „Stapelbar“. Dateiformat live per SSH byte-verifiziert (CRLF-Zeilenenden, `Group Name`-Kopfzeile mit Leerzeichen statt Tab, sonst wie `mob_drop_item.txt`), Round-Trip-Test reproduziert die echte Beispieldatei byte-exakt. Gruppen mit `Type attr` (Attribut- statt Item-Vergabe, komplett anderes Schema) werden bewusst mit klarer Fehlermeldung abgelehnt statt geraten. Noch nicht live in der laufenden App durchgeklickt
- [x] **Kisten-Editor: Neustart-Hinweis + Live-VNUM-Prüfung** (2026-08-06) — Nutzer meldete, eine frisch angelegte Kiste liefere beim Öffnen immer „Du hast nichts erhalten“ und werde dabei nicht verbraucht. Root Cause im Quellcode verifiziert: `special_item_group.txt` wird wie `refine_proto` nur beim Server-Start eingelesen (`input_db.cpp`), und `cmd_gm.cpp`s `/reload`-Befehl hat dafür keinen Unterfall — ein Neustart ist der einzige Weg, eine neue/geänderte Gruppe wirksam zu machen. Der Editor hatte dafür (anders als der Aufwertungs-Editor) bisher keinen Warnhinweis; jetzt nachgetragen (gleiches Banner-Muster, Link zur Server-Steuerung). Zusätzlich zeigt ein neuer `BoxVnumHint` sofort den echten `item_proto`-Namen/Typ zur eingetragenen Kisten-Item-VNUM inkl. Warnung bei falschem Typ/unbekannter VNUM — eine falsche VNUM löst denselben stillen Fehlschlag aus (`GiveItemFromSpecialItemGroup` sucht rein über die Box-VNUM, kein Namens-Abgleich)
- [x] **Item Editor: Beschreibung/Kurzbeschreibung** (`src-tauri/src/itemdesc.rs`, 2026-08-07) — Nutzerfrage, wo man einem Item eine Beschreibung geben kann. Recherche ergab: es gibt gar keine `item_proto`-Spalte dafür — die Tooltip-Beschreibung ist reiner Client-Text in `locale/<lang>/itemdesc.txt` (`CItemManager::LoadItemDesc` im echten Client-Quellcode), tab-getrennt `vnum\tBeschreibung\tKurzbeschreibung`. Byte-verifiziert direkt gegen die echte Datei im Client-Ordner des Nutzers (LF-Zeilenenden, Windows-1252, ein kleines Set Zeilen hat eine vom Client ignorierte 4. Spalte, die beim Speichern erhalten bleibt). Zwei neue Felder im Item Editor, eigener Pipeline-Schritt, reine lokale Dateioperation (kein SFTP, kein Server-Neustart nötig)
- [x] **Quest Builder: Vorlage "Item mit festen Boni verschenken"** (2026-08-07) — Nutzerfrage, ob eine Kiste ein Item mit vorgegebenen Bonus-Attributen (z.B. "Schwert+9 mit INT 500, STR 700") ausgeben kann. Im Quellcode verifiziert: die Kisten-Loot-Tabelle (`special_item_group.txt`) kann das nicht (nur VNUM+Anzahl+Chance), aber Quest-Lua kann es bereits — `pc.give_item2_select` (setzt anders als `pc.give_item2` das neue Item als "aktuelles Item") gefolgt von `item.set_value(index, apply_type, value)` (gebunden an `CItem::SetForceAttribute`) schreibt direkt in einen der 7 Bonus-Attribut-Slots des Items. Neue Vorlage im Quest Builder mit bis zu 4 Attribut-Slots (Dropdown = dieselben `EApplyTypes`-Werte wie `applytype0-3`), keine Server-Quelltext-Änderung nötig
- [x] **Kisten-Editor: Kisten-Item direkt suchen & einrichten** (2026-08-07) — Nutzerwunsch, das manuelle "im Item-Editor Typ/Stapelbar setzen" direkt im Kisten-Editor erledigen zu können. Neuer `BoxItemPicker`: Item-Suche (wiederverwendet `search_items`) + Ein-Klick-Einrichten (`get_item_proto` → `type=23`, `flag`-Bit "Stapelbar" setzen, `antiflag`-Bit "Nicht stapelbar" löschen → `update_item_proto` → `regenerate_item_proto`/`deploy_item_proto`, exakt derselbe DB+Client-Repack-Schritt wie im Item Editor, kein Server-Neustart nötig da item_proto eine normale DB-Prototabelle ist). Bestätigungsdialog, falls das gefundene Item noch nicht Typ GIFTBOX ist (Typänderung an einem bestehenden Item ist nicht risikofrei bei falscher Auswahl). Reine Frontend-Erweiterung, keine neuen Backend-Commands nötig
- [x] **Diagnose-Runde: Kisten-Wahrscheinlichkeit, Waffen-Grafik-Regression, Umbenennen-Pipeline, Kisten-Vergabe** (2026-08-07, keine Code-Änderung außer live reparierten Client-Dateien) — vier vom Nutzer gemeldete Probleme untersucht:
  - **"5 von 6 Öffnungen nichts erhalten" bei der Göttlichen Kiste (vnum 500001)**: `CSpecialItemGroup::GetMultiIndex`/`GetOneIndex` im Server-Quellcode exakt nachvollzogen. Eine Gruppe **ohne** `Type`-Zeile ("Normal", ihr Fall) trifft rechnerisch **immer** einen Eintrag — kein Fehlschlag möglich. Nur `Type pct`-Gruppen können "nichts" ergeben (jeder Eintrag rollt unabhängig). `iRarePct` wird im Server für den normalen Item-Vergabepfad nachweislich gar nicht ausgewertet (totes Funktionsargument). Wahrscheinlichste Erklärung für den Nutzer-Report: Server-Neustart fiel mitten in die Testreihe. Für Mehrfach-Öffnungs-Tests künftig `/i <vnum> <anzahl>` mit Anzahl verwenden, sonst gibt's pro Test nur eine Kiste.
  - **Waffen ohne Grafik ("wieder")**: FrostEdge (700010-700016) per GR2-Sidecar + echtem `EterPackConsoleLz4.exe item.eix`-Entpacken in Scratch-Ordner geprüft — Modell **und** Textur korrekt im gepackten `item.epk`. newitemswaffen (700000-700006, gemeinsame Textur `free_firedragon_zrye_weapon.dds`) hatte die Textur tatsächlich an ihrem virtuellen Zielpfad **fehlend** (nur Backups vorhanden, keine Live-Datei) — kein verantwortlicher Code-Pfad gefunden (`teardown_item` fasst Texturen nachweislich nie an), vermutlich manuell gelöscht. Live repariert: Textur aus der Sibling-Kopie wiederhergestellt, `item.epk` neu gepackt, im frisch gepackten Archiv verifiziert.
  - **Waffen-/Item-Umbenennen (locale_name) zeigt sich angeblich nicht im Client**: komplette Pipeline nachverfolgt (Mysql2Proto packt `locale_name`→`szLocaleName`, `CItemData::GetName()` im echten Client-Quellcode liest genau dieses Feld, `replace_client_item_proto` überschreibt die lose Datei in jedem Locale-Ordner unconditional bei jedem Speichern, kein `locale_de`-Pack existiert das die lose Datei überschatten könnte). Kein Bug gefunden — die Pipeline sollte bereits funktionieren. Nutzer gebeten, einen **echten** Client-Neustart (nicht nur Relogin) zu testen.
  - **Kisten-Vergabe-Werkzeug ("X Öffnungen direkt vergeben")**: recherchiert, ob es einen sicheren Weg gibt, ein Item mit gewählter Anzahl an einen Account zu vergeben. Der dafür vorgesehene Server-Mechanismus (`player.item_award`-Tabelle, vom Mall/Cash-Shop-System genutzt) ist auf diesem Server **komplett unbenutzt** — es existiert keine Quest, die eine `item_award`-Zeile tatsächlich zustellt (Volltextsuche über alle Quest-Dateien: null Treffer für "informer"/"item_award"). Ein Insert dort würde also nichts sichtbares bewirken. Mit dem Nutzer geklärt: nicht gebraucht — die Kiste wird später ein Boss-Drop (Mob-Drop-Editor deckt das ab), zum eigenen Testen reicht der GM-Befehl `/i <vnum> <anzahl>`. **Kein Werkzeug gebaut.**
- [x] **Generische Aufwertungs-Boost-Schriftrollen + Server-Quellcode Bauen & Einspielen** (`src-tauri/src/build_deploy.rs`, `src-tauri/src/deploy_history.rs`, `src/features/build-deploy`, 2026-08-07) — Nutzerfrage, ob andere Items die Aufwertungs-Erfolgschance erhöhen können (ja, per Aufwertungs-Schriftrollen, `CHARACTER::DoRefineWithScroll` in `char_item.cpp`, aber die 7 bestehenden sind hart im C++-Code einprogrammiert), plus der Wunsch, neue Boost-Items ohne weitere Neukompilierung anlegen zu können. Vollständig geplant (`/plan`, mit dem Nutzer abgestimmt) und umgesetzt:
  - **Schriftrollen-Patch**: neuer Zweig in `DoRefineWithScroll` für `value0 >= 7` (kollidiert nie mit den 7 Alt-Schriftrollen) — `value2` = Erfolgschance in %, `value3` = Verhalten bei Fehlschlag. Läuft bereits über den bestehenden generischen `REFINE_TYPE_SCROLL`-Client-Pfad, keine Client-Änderung nötig (verifiziert: alle Nicht-Musin/Hyuniron/Drachenblut-Schriftrollen liefen schon vorher darüber). Patch lokal UND live auf dem Server eingespielt (mit Backup der Originaldatei) — wirkt erst nach dem ersten echten Bauen & Einspielen. Hinweistext dazu im Item Editor bei USE_TUNING-Items.
  - **Server-Quellcode Bauen & Einspielen** (komplett neuer Bereich): der Live-Server (172.16.0.64, FreeBSD 14.3) wurde direkt per SSH untersucht, nicht angenommen — Quellcode liegt unter `/usr/home/source/server/`, Compiler `cc`→clang 19.1.7, `gmake` nötig (nicht BSD-`make`), **alle 4 Channels + der Login-Server zeigen per Symlink auf dieselbe eine `game`-Programmdatei** (`db` ebenso auf eine gemeinsame Datei) — kein Testserver, jede gebaute Version betrifft sofort den kompletten Live-Server. Deshalb: Bauen läuft **immer** in einer separaten, rsync-synchronisierten Arbeitskopie (nie im Live-Quellbaum), das Einspielen selbst verlangt als erstes Feature im Tool eine **eingetippte** Bestätigung (`SERVER ERSETZEN`/`ROLLBACK`, nicht nur geklickt), sichert automatisch die aktuelle(n) Programmdatei(en) vor jedem Einspielen, prüft danach mit zwei zeitversetzten `ps`-Momentaufnahmen ob der Prozess wirklich stabil läuft (nicht nur "irgendwas läuft" — erkennt eine Absturzschleife), und protokolliert jeden Vorgang in einem neuen Verlauf mit Ein-Klick-Rückgängig-machen. Noch nicht live gegen die echten Programmdateien getestet (nur Bauen gegen die Arbeitskopie ist bereits gefahrlos nutzbar) — der erste echte Einspiel-Vorgang mit der neuen Schriftrollen-Änderung steht noch aus.
- [x] **Account-Verwaltung: Liste, Anlegen, Passwort zurücksetzen** (`src-tauri/src/db/account.rs`, 2026-08-07) — Nutzerwunsch: statt nur Suche eine durchblätterbare Liste aller Accounts, schnelles Anlegen neuer Accounts (Login+Passwort), und Passwörter "auslesen" können. Live gegen die echte DB verifiziert: `account.password` ist ein MySQL-`PASSWORD()`-Einweg-Hash (41-Zeichen `*...`, geprüft an echten Accounts), exakt was der reale Login-Check (`input_auth.cpp`/`db.cpp`: `SELECT PASSWORD('eingabe'),password ... ` dann `strcmp`) erwartet — Auslesen ist technisch unmöglich, mit dem Nutzer auf "Zurücksetzen" geeinigt. Nebenfund beim Live-Check: ein echter Test-Account (`teststeven`) hat einen 60-Zeichen bcrypt-Hash (`$2a$10$...`) statt des erwarteten MySQL-Hashes — vermutlich von einem anderen (Web-)Tool angelegt, kann sich mit diesem Passwort-Format nie normal einloggen; nicht behoben, nur dokumentiert falls ein "Login geht nicht"-Report dazu aufkommt. Neue eigene Commands (`list_accounts`/`count_accounts`/`create_account`/`reset_account_password`) statt des generischen Zeilen-Editors, da `PASSWORD()` als SQL-Funktion aufgerufen werden muss, nicht als Klartext-Spaltenwert einsetzbar ist. Voller Round-Trip-Test gegen die echte Dev-DB (anlegen → suchen/listen → Passwort zurücksetzen → doppelten Login ablehnen → aufräumen).
- [x] **Server-Events + `EVENT.md`** (`src-tauri/src/db/event_flags.rs`, `src/features/server-events`, 2026-08-07) — Nutzerwunsch: ein Tab für Server-Events ("Doppel-Drop" etc.) zum An-/Ausschalten und Prozente einstellen, plus Recherche/Doku wie sich neue Events erstellen ließen und ob ein genereller Event-Ersteller möglich wäre. Mechanismus komplett im echten Quellcode verifiziert (`questmanager.cpp`, `item_manager.cpp`, `cmd_gm.cpp`): Event-Flags sind eine generische `map<string,int>` ohne feste Namensliste, persistiert in derselben `player.quest`-Tabelle wie Spieler-Quest-Fortschritt (`dwPID=0`), aber **nur einmal beim DB-Start geladen** — eine reine DB-Änderung wirkt wie bei `refine_proto` erst nach Server-Neustart, der Ingame-GM-Befehl `eventflag <name> <wert>` dagegen sofort (schreibt gleichzeitig in dieselbe Tabelle). Zwei grundverschiedene Wert-Semantiken live+quellcode-verifiziert und im Tab entsprechend unterschiedlich erklärt: Raten-Multiplikatoren (`mob_item`/`mob_exp`/`mob_gold`/`mob_gold_pct`/`mob_dam`/`user_dam` + 5 Premium-Varianten, ungesetzt = 100 = normal) vs. 17 saisonale Sonder-Drop-Flags (ungesetzt = Event komplett aus, "je kleiner der Wert desto häufiger"-Formel `40000×Stufenfaktor÷Wert`, mehrere mit zusätzlichen hart einprogrammierten Bedingungen wie Mindestlevel). Tab bietet für jeden Wert zusätzlich einen kopierbaren `eventflag`-Befehl für sofortige Wirkung, plus einen offenen Bereich für beliebige weitere/eigene Flag-Namen. `EVENT.md` (neu, Projekt-Wurzel) dokumentiert das Muster hinter den bestehenden 17 Sonder-Drop-Events (jedes ein eigener `if`-Block in `item_manager.cpp`), schätzt den Aufwand für ein wirklich neues Event auf unter eine Stunde dank des neuen Bauen-&-Einspielen-Werkzeugs, und bewertet einen generischen "Event-Ersteller ganz ohne Quellcode" ehrlich als teilweise möglich (Stufe 2: eine einmalige generische "Sonder-Drop aus DB-Konfiguration"-Erweiterung, analog zum Aufwertungs-Schriftrollen-Patch — noch nicht gebaut) und teilweise nicht realistisch als ein einziges Werkzeug (Stufe 3: visuelle Effekte/Sonder-Regeln sind strukturell zu unterschiedlich).
- [x] **Bugfix: Bauen & Einspielen brach bei `libsql` mit `'mysql/mysql.h' file not found` ab** (2026-08-07, echter Nutzer-Bugreport beim ersten Live-Test) — Ursache gefunden und live verifiziert: jedes Makefile bindet Drittanbieter-Bibliotheken über `-I../../../extern/FreeBSD/...` ein, ein relativer Pfad, der voraussetzt, dass `extern/` neben `server/` liegt (wie im Live-Baum `/usr/home/source/{server,extern}`) - die Arbeitskopie hatte bisher nur `server/` synchronisiert, `extern/` fehlte komplett. Fix: `sync_build_source` legt jetzt zusätzlich einen Symlink `extern -> <Live-extern>` neben der Arbeitskopie an (kein Kopieren, da nie verändert und groß). Fix live gegen den echten Server verifiziert: `libsql`, `game` und `db` bauen jetzt alle sauber in der Arbeitskopie durch (exit 0). **Nebenbefund, wichtig für den Nutzer geklärt:** der Nutzer hatte als Workaround `gmake clean && gmake` direkt im Live-Quellbaum (`/usr/home/source/server/game/src`) ausgeführt und damit die Live-`game`-Datei ohne jedes Backup ersetzt (Werkzeug-Sicherheitsnetz nur bei Nutzung über das Tool aktiv, nicht bei manuellem SSH-Bauen). Da noch kein Neustart erfolgte, liefen die 5 Server-Prozesse zum Zeitpunkt der Prüfung noch mit der alten Programmdatei im Speicher - nichts war akut gefährdet, aber ohne Rückfallpunkt bei einem künftigen Neustart. Auf Wunsch des Nutzers nachträglich manuell gesichert: `game.20260807_144846.bak` (die manuell gebaute Version) und `db.20260807_144846.bak` (unverändertes Original) in den jeweiligen `m2manager_backups`-Ordnern - beide Live-Dateien blieben dabei unangetastet an ihrem Platz, es wurde nur kopiert. Diese manuellen Backups sind **nicht** im `deploy_history`-Verlauf des Tools verzeichnet (kein Datenbank-Eintrag, da nicht über `run_deploy` gelaufen) - ein Wiederherstellen daraus müsste ebenfalls manuell per SSH passieren, der "Rückgängig machen"-Knopf im Tab kennt sie nicht.
- [x] **Quest Builder: Mehrschritt-Vorlage** (`questTemplates.ts`, `QuestBuilder.tsx`, 2026-08-07) — Nutzerwunsch: Quest-Erstellung soll so einfach werden, dass wirklich jeder eine eigene Quest bauen kann, auch mehrstufige. Auf Rückfrage entschieden, zuerst den visuellen Baukasten zu bauen (ein Freitext-"semi-KI"-Assistent ohne echte KI-Anbindung ist der nächste Schritt). Neue Vorlagenart "Mehrschritt-Quest" verkettet beliebig viele der bekannten Bausteine (Dialog/Sammeln/Töten/Item benutzen, per Array-Editor wie bei den Dungeon-Etagen hinzufügbar/entfernbar) zu einer echten mehrstufigen Quest ohne Code - jeder Schritt ein eigener `state`-Block, Übergang per `set_state()`. Erste Version vermied diese Syntax bewusst (nur aus dem Community-Wiki bekannt, nicht verifiziert); der Nutzer wies darauf hin, dass das Wiki aus denselben Server-Dateien aufgebaut wurde, die er nutzt - daraufhin im echten Quellcode geprüft statt einfach geglaubt: `set_state`/`setstate` sind in `questlua_quest.cpp` real registriert und rufen über `GetQuestStateIndex`/`PC::SetQuestState` (`questpc.cpp`) exakt den vom Wiki beschriebenen Mechanismus auf; `__COMPLETE__` ist dabei nur Namenskonvention, keine Engine-Sonderbehandlung. Noch am selben Tag entsprechend umgebaut (kein Fortschritts-Zähler mehr, echte `state`/`set_state`-Blöcke plus ein leerer `state __COMPLETE__`-Abschluss). Ebenfalls verifiziert: `pc.getqf`/`pc.setqf` sind quest-global, nicht pro Zustand gescoped - Kill-Schritte behalten deshalb ihren pro Schritt eigenen, namensraum-isolierten Zähler (`kill_count_stepN`), damit zwei Kill-Schritte in derselben Quest sich nicht gegenseitig überschreiben (per Regressionstest abgesichert). Kein Backend-Code nötig (`create_quest_file` nimmt bereits einen opaquen Content-String). 8 Vitest-Tests, zweimal live im Dev-Server geprüft (vor und nach dem Umbau: Vorlagenwechsel, Schritt hinzufügen/entfernen, Schritt-Art wechseln, generierte Vorschau Zeile für Zeile verifiziert).
- [x] **Quest Builder: Freitext-Assistent ("semi-KI")** (`questDescriptionParser.ts`, `QuestBuilder.tsx`, 2026-08-07) — zweiter Teil des Nutzerwunsches nach dem Mehrschritt-Baukasten: eine Freitext-Beschreibung soll die Quest automatisch vorausfüllen können, ausdrücklich ohne echte KI-Anbindung. Umgesetzt als reine Mustererkennung: Klausel-Split auf Satzzeichen und Verbindern (`dann`/`danach`/`anschließend`, auch als bloßes Komma vor einer erkennbar neuen Aktion - live getestet und dabei einen echten Bug gefunden: die erste Version splittete bei einem reinen Komma ohne "dann" gar nicht, genau das Muster aus dem eigenen UI-Platzhaltertext "Rede mit Hans, sammle 10 Wolfsfelle, dann ..." - gefixt, mit Regressionstest), Schlüsselwort-Klassifizierung in die 4 bekannten Schritt-Arten (dialog/collect/kill/use), Fuzzy-Namensauflösung über die bereits vorhandenen `search_items`/`search_mobs`-Befehle (die keine Relevanz-Sortierung liefern, quellcode-verifiziert - eigene `pickBestMatch`-Bewertung: exakt > Präfix > eindeutig kürzester Teilstring-Treffer, sonst `null` statt falsch geraten). Ergebnis füllt exakt den bereits bestehenden Mehrschritt-Baukasten (`MultiStepFormState`/`QuestStep`) - kein neuer Codegen-/Submit-Pfad, "Anlegen" bleibt die einzige Bestätigung. Unsichere Treffer (kein NPC/Item gefunden, keine Anzahl erkannt, unbekanntes Aktions-Verb) werden als Hinweise angezeigt statt stillschweigend übernommen. Reine Parsing-Logik von der async DB-Auflösung getrennt (`buildDraftSteps` synchron+getestet, `resolveDescription` nimmt eine injizierte Lookup-Funktion - dadurch auch mit einer gefakten Lookup-Funktion vollständig testbar, kein echter Tauri-Zugriff nötig). Kein neuer Backend-Code. 25 neue Vitest-Tests, live im Dev-Server mit festen `search_items`/`search_mobs`-Testdaten geprüft (genau das Beispiel aus dem UI-Platzhaltertext, Ende-zu-Ende von Freitext bis zur fertigen Mehrschritt-Quest-Vorschau).
- [x] **Freitext-Assistent + Mehrschritt-Baukasten: 4 Verbesserungen aus einem echten Test-Fall** (`questTemplates.ts`, `questDescriptionParser.ts`, `QuestBuilder.tsx`, 2026-08-07) — Nutzer testete den Assistenten mit einem realen Satz ("Rede mit dem Gemi, bringe der Gemi, 20 Orkzähne und erhalte dafür 250HP zusätzlich dauerhaft, du kannst diese quest alle 4 Tage machen") und meldete, dass die NPC-/Item-Suche fehlschlug. Alle vier daraus abgeleiteten Verbesserungen quellcode-verifiziert umgesetzt:
  - **Wiederholbare Quests mit Cooldown**: neue Felder `repeatable`/`cooldownDays` im Mehrschritt-Baukasten. `get_time()`/`get_global_time()` als echte, registrierte Quest-Funktion in `questlua_global.cpp` verifiziert. Schritt 1 wird per zusätzlicher `with`-Bedingung gesperrt (samt negiertem "noch nicht verfügbar"-Geschwisterblock pro eindeutigem Trigger, funktioniert auch wenn Schritt 1 vom Typ "Töten" ist mit 2 Chat-Triggern), der letzte Schritt setzt `pc.setqf("last_complete", get_time())`, setzt alle Töten-Zähler der Kette zurück und springt mit `set_state(start)` statt `set_state(__COMPLETE__)` zurück - der `state __COMPLETE__`-Block entfällt dann ganz (unerreichbar).
  - **Feste Bonus-Attribute als Belohnung**: `QuestStep` bekommt `rewardAttrType0-3`/`rewardAttrValue0-3` (gleiches Muster wie die schon bestehende `buffed_item`-Vorlage). Quellcode-Recherche ergab: es gibt keine saubere Quest-Funktion für "Max-HP dauerhaft erhöhen" (`pc.set_ht` verändert nebenbei die freien Stat-Punkte) - der reale Weg ist ein Item mit fest eingebautem Bonus (`APPLY_TYPES` hat bereits `Max. HP`), jetzt direkt als Belohnungs-Option im Baukasten statt nur in der Einzel-Vorlage.
  - **Bessere Satz-Erkennung**: `REWARD_MARKERS` um reale Konjugationen erweitert (vorher fehlte alles außer der Grundform), neues `REPEATABLE_PATTERN` erkennt "alle N Tage" als eigene Klausel und trägt sie in die neuen Wiederholbar-Felder ein statt als Textwust an die letzte Aktion zu hängen - genau der Bug, der im gemeldeten Beispiel auftrat.
  - **Manuelle Suche bei fehlgeschlagener Erkennung**: `resolveDescription` liefert zusätzlich eine strukturierte `unresolved`-Liste (Schritt/Feld/Suchbegriff), jeder Eintrag bekommt in der UI einen "Suchen"-Knopf, der denselben Picker wie jedes VNUM-Feld öffnet, mit der erkannten Phrase vorausgefüllt und automatisch durchsucht (`openPicker` um einen optionalen Anfangs-Suchbegriff erweitert).
  - Live mit dem exakten gemeldeten Satz getestet: NPC "Gemi" löst jetzt korrekt auf, "alle 4 Tage" wird korrekt als wiederholbar+Cooldown erkannt statt als Rauschen, und der "Suchen"-Knopf wurde end-to-end durchgeklickt (Item ausgewählt, korrekt im richtigen Feld übernommen). Ehrlich dokumentiert, was weiterhin eine echte Grenze ist: "bringe der Gemi, 20 Orkzähne" trennt Empfänger und Menge/Item nur durch ein Komma (kein "bei") - dieser rein grammatische Fall (indirektes vs. direktes Objekt) bleibt ohne echtes Sprachverständnis nicht zuverlässig auflösbar, dafür ist genau der neue "Suchen"-Knopf das Sicherheitsnetz. 24 neue Tests (85 insgesamt).
- [x] **Quest-Wiki** (`src/features/quest-builder/wiki`, 2026-08-07) — Nutzerwunsch: eine separat zu öffnende Wiki-Seite im Quest Builder, Inhalt 1:1 aus [wielandino/metin2-quest-docs](https://github.com/wielandino/metin2-quest-docs) übernommen, mit Quellenangabe. Vor dem Kopieren geprüft (nicht angenommen): Ziel-Repo ist privat, Quell-Repo hat keine LIZENZ-Datei (also formal alle Rechte beim Autor) — mit dem Nutzer abgestimmt, dass eine private, klar attribuierte Kopie in Ordnung ist. "Separat öffnen" stellte sich als echtes zweites Betriebssystem-Fenster heraus (nicht nur ein weiterer Sidebar-Bereich), ebenfalls mit dem Nutzer geklärt. Umsetzung: alle 15 Original-Markdown-Seiten unverändert per `gh api` heruntergeladen (Bytegröße gegen GitHub verifiziert) und per Vite-`?raw`-Import als Strings gebündelt; neuer, komplett eigenständiger Vite-Entry-Point (`wiki.html`/`src/wiki-main.tsx`) und ein zweites Tauri-`WebviewWindow` (Label `wiki`, eigene Capability `wiki.json` mit nur `core:default`+`opener:default`) statt eines Lazy-Route innerhalb des Hauptfensters, damit die Wiki-Seite parallel zum Schreiben eines Quests offen bleiben kann. Rendering über `react-markdown`+`remark-gfm`(Tabellen)+`remark-frontmatter`(entfernt Docusaurus-YAML-Köpfe), da das Projekt kein Typography-Plugin nutzt, jedes Element von Hand im bestehenden Tailwind-Stil gestylt. Sidebar-Navigation/Gruppierung 1:1 aus der echten `sidebars.js` des Original-Repos übernommen (Einführung, Erste Schritte, Grundkonzepte, 8-teilige API-Referenz, Benutzeroberfläche, Fortgeschrittene Themen, Best Practices, Kurzreferenz), zusätzlich Volltext-Suchfilter über Titel+Seiteninhalt. Live im Browser gegen den Vite-Dev-Server geprüft: Navigation/Gruppierung, Markdown-Rendering (Überschriften, Fettdruck, Listen, Inline-Code), Fenced-Code-Blöcke, GFM-Tabelle (`core-concepts.md`), Such-Filterung und die Quellenangabe-Fußzeile funktionieren alle wie vorgesehen.
- [x] **System-Installer** (`src/features/system-installer`, `src-tauri/src/{system_patch,system_scan,system_installs}.rs`, 2026-08-07, neue Sidebar-Kategorie "Systeme") — Nutzerwunsch: fertige Community-"Systeme" (Server-/Client-Erweiterungen) automatisiert einbauen statt jede Datei von Hand abzugleichen; bei Problemen soll sich der Einbau schnell wieder rückgängig machen lassen. Vor der Planung beide echten Beispielsysteme des Nutzers gelesen (`C:\Users\DevSteven\Desktop\Systeme`, ResizeWindow1.2 + zweiteiliges InGame-Admin) — dabei eine erstaunlich einheitliche, community-weite `// search` / `// add above|below|inside|at the end`-Konvention gefunden (viele Schreibvarianten: `##search:`, `//add ABOVE, not below:`, `##inside:` als Scope-Anker für nachfolgende Blöcke, bloßes `// add`, `# add at the end of this file`), die von zwei komplett unabhängigen Autoren genutzt wird. Kern ist ein reiner, 137-fach getesteter Parser (`system_patch.rs`) mit abgestufter Anker-Suche (exakt → einrückungs-tolerant → kommentar-bereinigt → Teiltreffer, behebt direkt das vom Nutzer genannte Problem "Kommentar mitten im Suchtext verhindert exakten Treffer") und Scope-Fenster-Auflösung bei mehrdeutigen Ankern. Zielpfad wird pro Datei über Dateiname-Suche vorgeschlagen (Server live per SSH über neue `ssh::find_remote_file_by_name`, Client-Quellcode über neue Einstellung `binary_src_path`, Client-Installationsdateien über bestehenden `client_path`) statt geraten, und ist jederzeit manuell überschreibbar; Freitext-Anweisungen/mehrdeutige/fehlende Treffer bleiben klar als "manuell nötig" markiert. Backup + Verlaufs-Tabelle (`system_installs.rs`, Vorbild `import_history.rs`) mit Ein-Klick-Rückgängig-machen, robust gegen Teil-Fehlschläge (bereits geschriebene Dateien werden trotzdem verzeichnet). **Live gegen die echten, unveränderten Beispieldateien beider Systeme verifiziert** (nicht nur gegen handgeschriebene Test-Fixtures) — dabei zwei echte Bugs gefunden und behoben: (1) ein `##add at the end of the file:`-Block **nach** bereits abgeschlossenen `##inside`/`##search`-Blöcken wurde fälschlich als Teil des vorherigen Blocks eingelesen statt als eigener `AppendToEnd`-Fall (nur der Sonderfall "einzige Kopfzeile der ganzen Datei" war vorher behandelt); (2) InGame-Admin liegt entpackt in einem zusätzlichen Wrapper-Ordner (`v1.2_.../ADDONS|Client|Source`, typisches .rar-Verhalten) — die Kategorisierung sah dadurch den Wrapper-Namen statt "Source"/"Client" als erstes Pfadsegment und ordnete alles falsch als Client-Installationsdatei ein; `scan_system_package` steigt jetzt transparent durch einen einzelnen Wrapper-Ordner ab. Struktur-Balance-Check (Klammern/`#if`-`#endif`) als eingebautes Sicherheitsnetz nach dem Einbau — ein vom Nutzer zusätzlich gewünschter eigenständiger, mehrsprachiger Syntax-Checker mit Auto-Reparatur-Vorschlag ist bewusst **nicht** Teil dieses Moduls (siehe Roadmap unten). **2026-08-08, drei Bugfix-Runden aus echten Nutzer-Tests:** stillschweigend verschluckte Fehler bei der Zielsuche + zu langsame Client-Suche (0.13.1), ein UTF-8/Windows-1252-Encoding-Bug beim lokalen Datei-Lesen (0.13.2), dann ein bisher unbekannter dritter Marker-Typ `//REPLACE with:` (0.13.3, real 6× im ResizeWindow1.2-Paket, u.a. 4× in einer Datei) — der Parser kannte ihn nicht, blieb im Anker-Erfassungszustand hängen und verschluckte den kompletten Rest der Datei lautlos (0 Ops statt echter automatisierbarer Blöcke), betraf mehrere Dateien im Paket. Neuer `Placement::Replace` implementiert. Dabei zusätzlich zwei noch gefährlichere Varianten gefunden: ein Tippfehler `// serach` hätte den nachfolgenden `add`-Marker fälschlich als "add ohne Suchtext" interpretiert und dadurch **still an der falschen Stelle** (Dateiende statt der eigentlich gemeinten Stelle) automatisch geschrieben — kein übersprungener Block, sondern eine unbemerkt falsche Automatik. Und `// little bit down add` (Marker-Wort am Satzende statt am Anfang) wurde komplett ignoriert. Beide jetzt abgefangen (Tippfehler-Alias bzw. Fallback auf `FreeformInstruction` zur manuellen Prüfung) - siehe CHANGELOG für Details.
- [x] **Sidebar-Kategorien neu strukturiert** (`src/store/navigation.ts`, 2026-08-08) — Nutzerwunsch: der Sammeltopf "Datenbank-Editoren" (12 von 22 Bereichen) war unübersichtlich geworden, während alle anderen Gruppen nur 1-3 Einträge hatten. Aufgeteilt in vier enger gefasste, thematische Gruppen (Items & Ausrüstung, Shops & Monster, Quests & Welt, Server & Accounts) - siehe CHANGELOG 0.13.2 für die genaue Zuordnung. Rein datengetrieben über `NAV_ITEMS`/`CATEGORY_ORDER`, Sidebar/Command-Palette brauchten keine eigene Änderung. Live im Browser durch alle 8 Gruppen gescrollt und geprüft.
- [x] **Handbuch** (`src/features/manual`, `manual.html`/`src/manual-main.tsx`, `src/lib/manual.ts`, 2026-08-08, neues Tauri-Fenster) — Nutzerwunsch: ein eingebautes Nachschlagewerk für M2Manager selbst (nicht die Quest-Lua-Doku), mit einem Hinweis-Knopf direkt in jedem Modul, der zum passenden Abschnitt springt. Nutzer-Entscheidungen (per Rückfrage geklärt): eigenes, parallel offen bleibendes Fenster wie das Quest-Wiki statt Sidebar-Bereich; alle ~23 Module sofort mit echtem Inhalt statt nur ein paar als Vorlage; der Hinweis-Knopf sitzt sichtbar bei jedem Modul-Titel (nicht nur ein globaler Knopf) - da keine gemeinsame Kopfzeilen-Komponente existiert (verifiziert: jede der ~23 Seiten schreibt ihr eigenes `<h1>`), war das eine kleine, mechanische Änderung in jeder Datei. Architektur 1:1 vom Quest-Wiki übernommen (eigener Vite-Entry, eigenes `WebviewWindow`, `react-markdown`+`remark-gfm`, eigene Capability `manual.json`) - Unterschied: Inhalt selbst verfasst (kein Attribution-Footer nötig), 24 Seiten (1 Einführung + 23 Module) gruppiert exakt wie die App-Sidebar, Seiten-IDs = echte `Section`-Werte aus `navigation.ts` (kein zusätzliches Mapping). Neu ggü. dem Quest-Wiki: **Deep-Linking** - beim Erstöffnen über den URL-Hash (`manual.html#item-editor`), bei bereits offenem Fenster über ein Tauri-Event (`manual-navigate`), das `Manual.tsx` per `listen()` empfängt. Live im Vite-Dev-Server geprüft (Navigation, Markdown-Rendering, Volltextsuche, Deep-Link per Hash bei frischem Laden) - dabei einen echten Robustheits-Bug gefunden: `listen()` wirft **synchron** (nicht nur als Promise-Rejection), wenn noch keine echte Tauri-IPC-Bridge existiert, was die ganze Seite leer ließ statt nur die Deep-Link-Events wegzulassen - jetzt mit `try/catch` abgesichert.
- [x] **Broadcast-System** (`src/features/broadcast`, `src-tauri/src/broadcast.rs`, 2026-08-09) — plant eine Rotation vorformulierter Ansagetexte über `notice_all()` statt manuellem `/n`. Lokale SQLite-Nachrichtenliste, Deploy generiert eine Quest-Datei (`Broadcast/Broadcast_System.lua`) über die bestehenden Quest-Builder-Befehle und lädt per `run_server_command` neu. Reale Server-Quelle vor dem Bauen geprüft: `/b` broadcastet auf diesem Server entgegen der Erwartung **nicht** (nur Echo an den GM selbst), `notice_all()` funktioniert echt global; `Reload()` löscht die Timer-Namenstabelle, löscht aber keine bereits gequeuten Timer-Events - abgefangen über revisionierte Timer-/Flag-Namen pro Nachricht.
- [x] **Tag/Nacht & Schnee (Wetter-Steuerung)** (`src/features/weather`, `src-tauri/src/weather.rs`, 2026-08-09) — schaltet die echten Event-Flags `day`/`xmas_snow` live um (Quest-Weg wie Broadcast-System, oder direkter GM-Befehl als Sofort-Alternative). Regen existiert serverseitig gar nicht als Code-Pfad, daher bewusst nicht angeboten (im Handbuch/UI klar benannt statt einfach zu fehlen).
- [x] **Broadcast-System/Wetter-Steuerung: zwei reale Bugs beim ersten Live-Deploy gefunden und behoben (2026-08-10)**: (1) Erste Bereitstellung in eine noch nie existierende Quest-Kategorie (`Broadcast/`, `Weather/`) schlug mit "No such file" fehl, weil `write_remote_file_with_backup` den Zielordner nie anlegte - jetzt allgemein gefixt (`ssh::ensure_remote_dir`, segmentweises `mkdir`, betrifft jeden Aufrufer dieser Funktion, nicht nur diese zwei Module). (2) Das Deploy-Log wirkte "sehr schwer zu sehen", weil beide Module entgegen ihrem eigenen Code-Kommentar ("gleicher Mechanismus wie Quest Builder") den `server-output`-Live-Event-Listener nie abonniert hatten und stattdessen die komplette Ausgabe erst ganz am Ende als einen einzigen Textblock reinkippten - jetzt identisch zu Quest Builder/Server-Steuerung mit `listen("server-output", ...)` behoben. (3) Direkt danach, dritte Meldung: Log-Bereich zeigte trotzdem "immer nur eine Zeile" - echter Ursache war ein Layout-Problem, kein Inhaltsproblem: anders als Quest Builder (dessen Log in einem eigenen `fixed inset-0`-Modal mit `max-h-[70vh]` sitzt, komplett losgelöst vom Seiteninhalt) war der Log-Bereich hier direkt in den normalen Seitenfluss inline gerendert (`h-full`/`flex-1` relativ zum seitenweiten scrollenden `<main>`) und musste sich den Platz mit variabel hohen Geschwistern (Nachrichtenliste, Vorschau) teilen - blieb dadurch auf einen Streifen zusammengequetscht. Behoben, indem beide Module exakt Quest Builders Modal-Muster übernehmen (`deployOpen`-State, automatisch beim Start geöffnet, "Ausgabe anzeigen"-Knopf falls geschlossen). Noch nicht erneut komplett live durchgetestet (siehe [[m2manager_broadcast_system]]).
- [x] **App-Icon ausgetauscht** (2026-08-10) — Nutzer lieferte ein neues Logo (`public/tauri.svg`, eigenständige "M2 Manager"-Bildmarke). SVG war nicht quadratisch (858×963pt) - `tauri icon` verlangt ein quadratisches Ausgangsbild, daher `width`/`height` im SVG-Root auf gleiche Werte gesetzt (nur Canvas-Maße, `viewBox`/Pfade unverändert; das bereits vorhandene `preserveAspectRatio="xMidYMid meet"` zentriert die Grafik automatisch mit transparentem Rand). Mit `npx tauri icon public/tauri.svg` alle Windows-/macOS-Icon-Größen neu erzeugt (`src-tauri/icons/`); generierte iOS-/Android-Ordner wieder entfernt (Projekt ist bewusst Windows-only, siehe [[m2manager_plan]]). Visuell geprüft (128px und 32px) - Logo bleibt auch klein erkennbar. `cargo build` läuft weiterhin fehlerfrei (icon.ico-Einbettung nicht kaputt).

---

## 🚧 Fehlt noch / offen

### Kurzfristig sinnvoll (nächste Schritte)
- [ ] **Item Editor v2** — Waffen/Rüstungen mit eigenem `.gr2`-Modell-Import (eigene Ausbaustufe, laut Nutzer bewusst nicht in v1)
- [x] **Item Editor: alle Flag-/Typ-Tabellen gegen Server-Quellcode verifiziert** — `type` (35 statt 6 Einträge), `subtype` (jetzt typabhängiges Dropdown über `SUBTYPES_BY_TYPE`, 14 Item-Typen abgedeckt), `flag` (neues Dropdown statt Rohwert), `wearflag`/`antiflag`/`immuneflag`/`applytype0-3`/`limittype0-1` — alles direkt aus `enum`s in `length.h`/`item_length.h` des echten Server-Quellcodes übernommen. Dabei mehrere echte Fehler in den vorherigen Rate-Tabellen gefunden und korrigiert: `WEAR_FLAGS` hatte Schild/Pfeile vertauscht, `ANTI_FLAGS` war für fast jedes Bit falsch (0x0001 war als "kein Krieger" gelabelt, ist aber FEMALE), und die vom Nutzer selbst gegebene applytype-Liste stimmte ab Wert 3 nicht (siehe [[m2manager_item_editor]])
- [x] **Item Editor: Waffen-/Rüstungs-Schadenswerte (value0-5) vollständig verifiziert** — server- (`battle.cpp`: `Item_GetDamage`/`CalcMeleeDamage`) **und** clientseitig (`Client/root/uitooltip.py`: `__AppendAttackPowerInfo` etc.) geprüft, beide stimmen überein: value3/value4 = Schaden min/max, value5 = Zusatz-Angriffskraft (fließt server- und clientseitig ein), value0 = Angriffstempo-Kategorie, value1/value2 = optionaler Magie-Angriff. Rüstung: value0=Magie-Verteidigung, value1=Verteidigung, value5=Zusatz-Verteidigung. Felder haben jetzt echte Labels statt "value0"/"value1", plus Live-Vorschau "so wird es im Client angezeigt" im Item Editor. Balance/Spieldesign (welche Zahl für welches Level "passend" ist) bleibt bewusst offen — dafür weiterhin "Referenz-Item übernehmen" nutzen (siehe [[m2manager_item_editor]])
- [ ] **Item Editor: value0-5 für alle anderen Typen (nicht Waffe/Rüstung)** — bleibt core-spezifisch, Bedeutung hängt von Typ **und** Subtyp ab (in `char_item.cpp` geprüft, z.B. 34 verschiedene USE-Subtypen mit je eigener Bedeutung). Kontext-Hinweis für USE vorhanden, aber nicht vollständig — „Referenz-Item übernehmen" bleibt der zuverlässigste Weg (siehe [[m2manager_item_editor]])
- [ ] **Weitere Sprachen** — README verspricht 13 Sprachen (FR, ES, PT, IT, NL, TR, RU, RO, CZ, HU, GR …), real vorhanden sind nur DE/EN. Entweder README korrigieren oder Sprachdateien nachziehen (separate Translation-Pipeline aus dem Plan ist noch nicht gebaut)
- [x] **Neuer geteilter `EntityBrowser` (paginiertes Durchblättern statt Nur-Exakt-Suche)** (`src/features/shared/EntityBrowser.tsx`, `db/shop.rs::browse_items`/`browse_mobs`, 2026-08-10) — Nutzerkritik: Item Editor und Aufwertungs-Editor waren nur nützlich, wenn man die exakte vnum kannte, da `search_items`/`search_mobs`'s Zahlen-Zweig `WHERE vnum = ?` macht (Volltreffer oder nichts, kein Fallback zum Durchblättern); gleiches für Mob-Proto-Editor gewünscht. Neue Backend-Funktionen sind paginiert (`offset`/`limit` + echte `total`-Anzahl) und broaden den Zahlen-Abgleich von exakt auf "enthält" (`CAST(vnum AS CHAR) LIKE '%query%'`) — sicher, weil durch die Pagination ein breiter Treffer nicht mehr wie vorher am festen `LIMIT` abgeschnitten wird; leere Suche = kompletter Bestand nach vnum sortiert. `search_items`/`search_mobs` selbst und ihre anderen Aufrufer (Quest Builder, Mob Drop Editor) bewusst unangetastet gelassen, um den Umbau auf die drei genannten Module zu begrenzen. Neue Komponente lädt beim Öffnen sofort eine erste durchblätterbare Seite (nicht erst nach einer Suche) und übernimmt DB Explorers bestehendes Pagination-Muster (Vor/Zurück, "X Treffer · Seite Y/Z"). Ersetzt in `RefineEditor.tsx` (alle 3 Stellen: Ziel-Item, Material, Haupt-Itemwahl), `ItemEditor.tsx` (Bearbeiten-Suche + Referenz-Item-Suche) und `MobProtoEditor.tsx` (Hauptsuche) jeweils die vorher dupliziert-eigene Such-UI. Alle 158 Rust-Tests grün, `tsc` sauber. **Noch nicht live gegen die echte Dev-DB getestet.**
- [x] **Dashboard-Statistiken** — bereits vollständig mit echten Live-Queries umgesetzt (`get_database_stats` in `db/shop.rs`, zählt Accounts/Spieler/Items/Shops/Mobs; `Dashboard.tsx` zeigt sie inkl. Refresh-Button) — beim Gegenprüfen am 2026-07-29 als bereits fertig bestätigt
- [x] **Bulk-Sync der Stack-Größen** über alle Shops — neu gebaut am 2026-07-29: `sync_shop_stack_sizes` (Tauri-Command, `db/shop.rs::sync_stack_sizes`) überschreibt `shop_item.count` entweder für den aktuellen Shop oder global für alle Shops auf den eingestellten Max-Wert. Button „Bestandsware synchronisieren" im Shop Editor neben dem Max-Stepper, mit Bestätigungsdialog (destruktive Bulk-Aktion) und Ergebnis-Anzeige (Anzahl aktualisierter Zeilen)
- [ ] **Automatisierte Tests für Server-Steuerung** — bisher bewusst nur mit harmlosen Commands (`echo`/`sleep`) getestet, echte Start/Stop-Läufe nie automatisiert verifiziert (siehe [[m2manager_server_control]])
- [ ] **Mob Drop Editor: Live-Test gegen echten Server** — Parser/Serializer sind gegen die echte Beispieldatei des Nutzers unit-getestet (Round-Trip), aber SFTP-Lesen/Schreiben/Backup wurde noch nie live gegen den Dev-Server ausgeführt (siehe [[m2manager_mob_drop_editor]])
- [ ] **Eigenständiger Syntax-Checker** — vom Nutzer beim System-Installer-Auftrag zusätzlich gewünscht: beliebigen Ordner wählen, alle Dateien auf Syntaxfehler prüfen, bei Fehlern automatisch einen Korrekturvorschlag anzeigen, Nutzer bestätigt oder lässt es so. Bewusst nicht Teil des System-Installers — der baut nur einen eingebauten Struktur-Check (Klammern/`#if`-`#endif`-Balance) als Sicherheitsnetz. Der volle Bereich (mehrsprachig, echte Auto-Reparatur-Vorschläge) ist ein eigenständiges, größeres Feature und noch nicht begonnen
- [ ] **System-Installer: erster echter Einbau + Rückgängig-machen live getestet** — Parser/Kategorisierung/Anker-Suche sind gegen die echten, unveränderten Dateien beider Beispielsysteme verifiziert (siehe oben, dabei zwei reale Parser-/Kategorisierungs-Bugs gefunden und behoben), der eigentliche Einbau/Rückgängig-machen-Zyklus wurde noch nicht durchgeklickt. **2026-08-08, echter erster Nutzer-Test:** zwei weitere reale Bugs bei der Zielsuche gefunden und behoben (siehe CHANGELOG 0.13.1) - Fehler wurden im Frontend stillschweigend verschluckt (jeder Fehlschlag sah wie "nichts gefunden" aus, egal ob `binary_src_path` fehlte oder ein Ordner statt einer Datei als Ziel angegeben war), und die Client-Zielsuche durchsuchte den kompletten `client_path`-Baum (58k Dateien beim Nutzer) einmal pro Paket-Datei statt gebündelt (live gemessen: 31,4s einzeln vs. 2,9s gebündelt für 11 Dateien) - neuer `find_system_targets_batch`-Befehl behebt das. `binary_src_path` war beim Test schlicht noch nicht gesetzt (Einstellungen → Client-Quellcode-Ordner) - jetzt mit sichtbarer Fehlermeldung statt stiller "nicht gefunden"-Anzeige. **Direkt danach, gleicher Test-Durchlauf:** neuer Fehler sichtbar geworden (Beweis, dass die vorherige Fehler-Anzeige jetzt wirklich greift) - "stream did not contain valid UTF-8" beim Lesen einer lokalen Zieldatei. Ursache: derselbe UTF-8/Windows-1252-Bug wie bei Quest Builder/Mob Drop Editor (siehe dort), bisher nur auf der SSH-Seite gefixt, nicht beim lokalen Datei-Lesen. Behoben (siehe CHANGELOG 0.13.2), inkl. eines verwandten zweiten Funds: `scan_system_package` hat nicht-UTF-8-Textdateien im Systempaket selbst bisher lautlos übersprungen statt sie zu lesen. **2026-08-10, fünfte Bugrunde — der eigentliche Blocker:** Nutzer meldete "lädt und lädt, es passiert absolut gar nichts" beim Scan von ResizeWindow1.2. Backend live gegen die echten Dateien des Nutzers durchgetestet (Diagnose-Test-Methode) und für einwandfrei befunden (5ms Scan, alle 27 Dateien korrekt geparst, Ziel-Suche gegen echten `client_path`/`binary_src_path` in ~3s erfolgreich). Ursache war ein **React-Stale-Closure-Bug** im Frontend: `resolveOps()` las den `items`-State direkt statt ihn als Parameter zu bekommen — da `resolveOps` aber immer tief aus der beim Klick auf "System-Ordner wählen" gestarteten Async-Kette aufgerufen wird, war `items` zu dem Zeitpunkt immer `null`/veraltet, wodurch `resolveOps` sofort zurückkehrte, ohne je einen Op aufzulösen. Jede Datei mit echten search/add-Blöcken blieb dadurch für immer auf "wird geprüft…" hängen. Behoben, indem die `ScannedFile` direkt als Parameter durchgereicht wird statt aus dem State neu aufgelöst zu werden. **Noch nicht erneut live im laufenden Tauri-Fenster verifiziert** (nächster Schritt für den Nutzer)

### Mittelfristig (im Plan vorgesehen, noch nicht begonnen)
- [ ] **Auto-Updates** — `tauri-plugin-updater` ist noch nicht als Dependency vorhanden, keine GitHub-Releases-Pipeline, kein Update-Badge in der UI
- [x] **Keyboard Shortcuts (teilweise)** — Command Palette (Strg+K, siehe oben) springt zu jedem Bereich; darüber hinausgehende Shortcuts (z.B. Speichern-Shortcut in Editoren) noch nicht gebaut
- [ ] **Translation-Pipeline-Tooling** (separates Dev-Tool: zentrale Translation-DB + LibreTranslate) — laut Plan komplett separates Repo/Tool, noch nicht begonnen
- [ ] **Release-/Build-Pipeline** — Windows-Installer/Portable-Exe-Build (`npm run tauri build`) noch nicht als CI/Release-Workflow eingerichtet

### Bewusst zurückgestellt (Architektur-Entscheidung, kein Bug)
- [ ] **Role-Based Access Control** — aktuell exakt 1 lokales Profil, keine Logins (Absicht)
- [ ] **Team Management / Sync** — Research-Thema für später, zentral gehosteter Sync-Service (Supabase/PocketBase/Turso-artig), Nutzer soll explizit **nicht** selbst hosten müssen — noch nicht designt

---

## Empfehlung für die nächste Session

Priorität, wenn du jetzt weitermachen willst:
1. **System-Installer: ersten echten Einbau im laufenden Tauri-Fenster durchklicken** — Parser/Kategorisierung sind bereits gegen die echten Beispieldateien beider Systeme verifiziert (siehe oben), aber die UI selbst (Ordner wählen, Ziel bestätigen, anwenden, Rückgängig machen) noch nicht, da der Ordnerauswahl-Dialog echtes Tauri braucht statt nur Vite-Dev-Server+Browser-Stub. `binary_src_path` muss dafür vorher in den Einstellungen gesetzt sein.
2. **Live-Test der neuen DB-/SSH-lastigen Features gegen den echten Dev-Server**: Mob-Proto-Editor, Account-/Spieler-Verwaltung und Datenbank-Backups (mysqldump/restore) wurden nur lokal gebaut/getestet, nie live verifiziert — ähnlich wie beim Mob Drop Editor. Insbesondere `server_process_names` (Ressourcen-Monitoring) und die Primärschlüssel-Erkennung für `account`/`player` verdienen einen echten Blick.
3. **Auto-Updates** aufsetzen (`tauri-plugin-updater` + GitHub Releases) — sobald du an einen Punkt kommst, an dem du Builds an andere verteilen willst
4. **README-Sprachliste korrigieren** (schneller Fix) oder echte i18n-Dateien für die fehlenden Sprachen ergänzen

RBAC/Team-Sync bewusst NICHT vorziehen — laut [[m2manager_plan]] ist das explizit ein späteres Research-Thema.
