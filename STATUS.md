# M2Manager – Status & Roadmap

Diese Datei ist die laufende Referenz für „was ist schon gebaut" vs. „was fehlt noch". Wird bei jeder größeren Session aktualisiert/abgehakt. Quelle der Ursprungs-Features: `Idee.md` + `weitere Informationen.md`.

Stand: 2026-08-03

---

## ✅ Fertig / funktionsfähig

- [x] **Projekt-Grundgerüst** — Tauri v2 + React/TS/Vite, Tailwind + shadcn-artige UI-Komponenten, Zustand-Store
- [x] **Dark/Light Theme** — `src/store/theme.ts`, `prefers-color-scheme`-Erkennung + manueller Override
- [x] **i18n-Grundgerüst** — react-i18next eingebunden, lädt bereits **Deutsch + Englisch** (`src/i18n/locales/{de,en}.json`)
- [x] **Connection Manager** — SSH- und MySQL-Profile inkl. „Verbindung testen", Secrets über Windows Credential Manager (`keyring`, `windows-native`-Feature — siehe [[m2manager_credentials_bug]]). **2026-07-29: eigener „Verbindungen"-Tab entfernt, Inhalt in die Einstellungen verschoben** — die Einstellungsseite ist jetzt in die Kategorien „Allgemein" / „Server" (SSH, MySQL, Mysql2Proto, VNUM-Bereich, Mob-Drop-Dateipfad) / „Client" (Client-Pfad, NPC-Liste, EterPackConsoleLz4) sortiert
- [x] **SSH-Backend** — `ssh.rs`, inkl. Fallback auf keyboard-interactive Auth (siehe [[m2manager_ssh_auth]])
- [x] **Path Configuration + Setup Wizard** — geführter Erststart (Pfade → SSH → MySQL), `SetupWizard.tsx`
- [x] **Server-Steuerung** — Start/Stop/Logs löschen/Quests reloaden über die interaktive `index.sh`, Live-Log-Streaming per Tauri-Events, Bestätigungsdialog bei destruktiven Aktionen. **Neustarten-Button** (2026-07-29): führt Stop- und Start-Befehl nacheinander aus mit konfigurierbarer Wartezeit dazwischen (Standard 5s, editierbar), eigener Bestätigungsdialog, keine neuen Backend-Commands nötig (nutzt `run_server_command` zweimal) (siehe [[m2manager_server_control]])
- [x] **DB-Schema-Mapping** — `db/mapping.rs`, verifiziertes Standard-Mapping gegen echten Dev-Server (siehe [[m2manager_db_schema]])
- [x] **Generischer Database Explorer** — freie Tabellenansicht für beliebige Tabellen (`DbExplorer.tsx`, `db/explorer.rs`)
- [x] **Shop Editor** — Grid-UI wie im Client, NPC-Auswahl mit 3D-Preview, Item-Suche/-Filter, Mengen-Steuerung, 40-Slot-Limit, Lösch-Bestätigung (siehe [[m2manager_item_icons]])
- [x] **Item-Icons** — Lookup aus `icon.epk`-Extract inkl. Decade-Fallback für Refine-Ketten
- [x] **3D Model Viewer (GR2)** — 32-Bit-Sidecar lädt Client-eigene `granny2.dll`, Rigid- **und** Skinned-Mesh-Extraktion, three.js-Rendering (`Gr2Canvas.tsx`), NPC-Textur-Anwendung (`textures.rs`) (siehe [[m2manager_gr2_reference]])
- [x] **Dashboard** — erste Version vorhanden (`Dashboard.tsx`)
- [x] **Lokale Settings-DB** — SQLite via `rusqlite`, `get_setting`/`set_setting`
- [x] **Item Editor v1 (ohne 3D-Modell)** — neue Items anlegen **und bestehende bearbeiten**: DB-Insert/-Update in `player.item_proto` (`db/item.rs`), Icon-Schreiben nach `pack/icon/icon/item/<vnum>.tga` (`packtools.rs`, beim Bearbeiten optional), automatisches Neu-Packen von `icon.epk` und Neuerzeugen/Einspielen von `item_proto` über die externen Tools `EterPackConsoleLz4.exe`/`Mysql2Proto.exe` (beide per CLI angesteuert, Backups vor jedem Überschreiben, Bestätigungsdialog vor der destruktiven Kette, Vorab-Prüfung der Tool-Pfade vor jedem DB-Write, automatischer Rollback bei Fehlschlag). Waffen/Rüstungen mit **neuem** 3D-Modell sind bewusst noch nicht Teil davon (v2) — siehe Plan `dapper-snuggling-cat.md` und [[m2manager_item_editor]]
- [x] **Mob Drop Editor** — lädt/bearbeitet/speichert `mob_drop_item.txt` direkt auf dem Server per SFTP (erste echte SFTP-Nutzung im Projekt, `ssh.rs::read_remote_file`/`write_remote_file_with_backup`, `russh-sftp` war seit Projektstart ungenutzte Dependency). Strikter Parser/Serializer (`mobdrop.rs`, parse-or-refuse, immer lückenlose Neu-Nummerierung der Drop-Einträge beim Speichern), Item-Auswahl wie im Shop Editor (Suche + Icons), vier Bulk-Änderungsmodi für Drop-Prozente (Delta, Fest, Zufallsbereich, bestimmtes Item überall) je mit Global/Nur-dieser-Mob-Scope, serverseitiges Backup vor jedem Überschreiben. Zeigt zusätzlich "≈X% real" pro Prozentfeld (aus dem echten Server-Quellcode verifizierte Formel: reale Chance ≈ Prozentwert ÷ 4 — **2026-08-03 korrigiert**: eine frühere Version hatte das ÷400-Ergebnis als Bruch statt als Prozentzahl behandelt und dadurch 100× zu niedrige Werte gezeigt, aufgefallen durch reale Spieltest-Beobachtung eines Nutzers, gegen `item_manager.cpp`/`item_manager_read_tables.cpp` nachgerechnet und in `dropChance.ts` mit Regressionstest fixiert) über ein "i"-Info-Popover ("Serverfiles von Fliege V3 - Fratello", erweiterbare Datenliste in `serverNotes.ts`). Zweiter Tab **"Lokale Datei prüfen/reparieren"**: wählt eine beliebige lokale `mob_drop_item.txt`-Kopie, validiert sie mit demselben strikten Parser, und bei Syntaxfehlern gibt es eine Rohtext-Recovery-Ansicht (Fehlermeldung mit Zeilennummer + Textarea + "Erneut prüfen") bis die Datei sauber einliest — danach im selben strukturierten Editor weiterbearbeitbar, Speichern schreibt lokal mit Backup — siehe [[m2manager_mob_drop_editor]]
- [x] **MySQL-Auto-Reconnect beim App-Start** — `src/lib/mysqlConnect.ts` verbindet automatisch mit den gespeicherten Zugangsdaten, bevor die UI überhaupt gerendert wird; vorher musste man nach jedem Neustart einmal in den Connections speichern, damit z.B. Shop Editor/Item Editor/Dashboard funktionierten (siehe [[m2manager_item_editor]])
- [x] **TGA Konverter** — eigener Tab, wandelt PNG/JPG/BMP/GIF beliebig gewählter Dateien in `.tga` um (Mehrfachauswahl, Vorschau, editierbarer Zielpfad pro Datei, "Zielordner für alle setzen"). Teilt sich die Konvertierungslogik (`imageconv.rs`) mit dem Item Editor — **die Icon-Konvertierung beim Item-Anlegen/-Bearbeiten lief technisch schon vorher automatisch**, war aber auf PNG/TGA als Eingabe beschränkt (Cargo-Feature-Flags); jetzt zusätzlich JPG/BMP/GIF, was beiden Stellen zugutekommt
- [x] **Regen-Datei-Editor** (2026-07-30) — bearbeitet die Monster-Spawn-Dateien, auf die Dungeon-Etagen im Quest Builder verweisen (`share/data/dungeon/**/*_regen.txt`). Format 1:1 aus dem Server-Quellcode (`regen.cpp::read_line`) verifiziert, nicht angenommen: `<typ> x y radius_x radius_y z_sektion richtung zeit prozent max_anzahl vnum`, Sonderfall `e` (Ausnahmezone) bricht nach 5 Werten ab. SFTP-Ordnerbrowser (neu: `ssh::list_remote_dir`, `russh_sftp::read_dir` war bisher ungenutzt) + strukturierter Editor pro Zeile (Spawn/Ausnahmezone/unverändert übernommene Kommentar- oder Leerzeilen), parse-or-refuse wie beim Mob Drop Editor, Backup vor jedem Speichern. Neue Einstellung `regen_base_dir` (siehe [[m2manager_quest_builder]])
- [x] **Locale-String-Verwaltung** (2026-07-30) — bearbeitet `share/translate.lua` (~8860 Zeilen), die Textbausteine, die Quests über `gameforge.questname._key` referenzieren (Format aus der Datei selbst dokumentiert, dort als offizielle "Gameforge-Way-To-Do"-Konvention beschrieben). Bewusst kein Voll-Parse/Reserialize der ganzen Datei — `write_namespace` ersetzt nur die Zeilen des bearbeiteten Namespace anhand von Präfix-Matching, der Rest von ~8800 nicht im Detail modellierten Zeilen bleibt byte-identisch erhalten. Neue/bestehende Namespaces, Keys hinzufügen/entfernen/bearbeiten, `[ENTER]`-Marker wird wie im Quest Builder als echter Zeilenumbruch in der Textarea angezeigt. Neue Einstellung `locale_file_path` (siehe [[m2manager_quest_builder]])
- [x] **Backup-Browser** (2026-07-30) — durchsucht die verstreuten `m2manager_backups`-Ordner (jeder Editor legt sein Backup direkt neben der Originaldatei an, kein zentraler Ort) und erlaubt Wiederherstellen einer Backup-Datei an ihren Ursprungsort — die aktuell dort liegende Datei wird dabei selbst zuerst gesichert statt einfach überschrieben. Nutzt denselben neuen `list_remote_dir`-Befehl wie der Regen-Datei-Editor
- [x] **Quest-Funktionsreferenz im Code-Editor** (2026-07-30) — durchsuchbares Seitenpanel mit allen 611 verifizierten Quest-Lua-Funktionen (`share/quest/quest_functions` vom echten Server), Klick fügt die Funktion an der Cursor-Position im Rohcode-Editor ein
- [x] **Bugfix: Namenssuche in Item-/Mob-Suche fand nur exakte Groß-/Kleinschreibung, Database Explorer ebenfalls behoben** (2026-07-30) — derselbe Fix wie unten bei Quest Builder beschrieben, zusätzlich auch in `db/explorer.rs::search_rows` (generischer Database-Explorer-Suche) angewendet, dort nur wenn die Spalte tatsächlich `binary`/`varbinary` ist
- [x] **Gemeinsamer Fehler-Helfer gegen stillschweigend verschluckte Fehler** (2026-07-30) — `src/lib/asyncAction.ts`: `runAsyncAction` macht `onError` zur Pflichtangabe, damit ein `try {...} finally {...}` ohne `catch` (der exakte Bug unten bei Mob Drop Editor/Quest Builder) strukturell nicht mehr passieren kann; an beiden Stellen bereits eingesetzt, mit eigenen Vitest-Tests abgesichert
- [x] **Frontend-Tests eingerichtet (Vitest)** (2026-07-30) — bisher gab es nur Rust-Tests; jetzt 24 Vitest-Tests für die heikelsten TS-Stellen: alle 6 Quest-Builder-Vorlagen-Generatoren (inkl. strukturellem `begin`/`if`/`end`-Balance-Check), die verifizierten Waffen-Tooltip-Formeln aus dem Item Editor, `asyncAction.ts`. `npm run test` bzw. `npx vitest run`
- [x] **Quest Builder** (2026-07-29) — verwaltet die echten Quest-Quelldateien auf dem Server per SFTP: Layout (`share/quest/<Kategorie>/<Name>.lua`, Liste in `quest_list`, Kompilierung über das vorhandene `qc_x64`/`make.py`) direkt auf dem Dev-Server verifiziert, nicht angenommen. Links Kategorie-/Datei-Browser (aus `quest_list` geparst), rechts Rohcode-Editor mit Speichern (SFTP-Backup vor jedem Überschreiben) und Löschen (kein Hard-Delete, Datei wandert ins Backup-Verzeichnis, Eintrag aus `quest_list` entfernt). "+"-Dialog erzeugt neue Quests aus 4 Vorlagen (NPC-Dialog+Belohnung, Sammel-Quest, Kill-Quest, Item-Benutzung) mit Item-/Mob-Auswahl (Suche wie im Shop/Mob-Drop-Editor, neuer `search_mobs`-Command); die generierte Lua-DSL-Syntax (`when NPC.chat."Label" [with Bedingung] begin…end`, `when MOB.kill`, `when ITEM.use`, `pc.give_item2`, `pc.getqf/setqf` usw.) stammt aus echten Quest-Dateien auf dem Server (`Pferd_rufen.lua`, `Orkzahn.lua`), nicht aus generischer Metin2-Doku. "Kompilieren & Neuladen"-Button nutzt denselben Befehl wie "Quests reloaden" in der Server-Steuerung und zeigt die Compiler-Ausgabe live. Vorlagen decken bewusst nur einfache Ein-Zustand-Quests ab — komplexe Multi-State-Quests bleiben Sache des Rohcode-Editors. **2026-07-30: 5. Vorlage "Run / Instanz-Dungeon"** ergänzt, nachdem der Nutzer einen "Runs Ersteller" anhand der 5 echten `.quest`-Dateien im `Runs`-Ordner wünschte: alle 5 echten Runs direkt vom Server gelesen (per SFTP-Diagnose) und analysiert — 3 davon (Beran_Eintritt, Daemonenturm, Teufelskatakomben) sind hochgradig individuelle Boss-Mechaniken (Passwort-Systeme, Timer-Ketten, Gruppen-Leader-Tracking) und bewusst **nicht** 1:1 nachbaubar; das gemeinsame Grundmuster aller Runs (mehrstufige Instanz: NPC-Einstieg → `d.regen_file`/`d.setf("level",N)` pro Etage → Boss-Kill schaltet nächste Etage frei via `d.jump_all`/`d.clear_regen` → letzte Etage vergibt Belohnung und wirft über `server_timer`+`d.exit_all()` raus) ist dagegen verifiziert und als Vorlage abgebildet — mit dynamischer Etagen-Liste (Boss-VNUM, Einstiegskoordinaten, Regen-Datei-Pfad pro Etage). Zwei Dinge muss der Nutzer selbst liefern (nicht aus den Quest-Dateien ableitbar): den Dungeon-Map-Index-Bereich (aus der Server-Dungeon-Konfiguration) und die Regen-Dateien selbst (eigenes Format, wird hier nicht erzeugt). Backend-Fix nebenbei: `create_quest_file` erlaubt jetzt eine wählbare Dateiendung (Runs nutzen `.quest`, normale Quests weiter `.lua`). **2026-07-30, Bugfixes nach erstem Nutzertest:** (1) Manche Quest-Dateien sind nicht UTF-8 sondern Windows-1252 (z.B. `Biologie/Biochecker.lua`) — Datei-Lesen brach bisher hart ab, liest jetzt mit UTF-8-zuerst-dann-Windows-1252-Fallback (betrifft auch Mob Drop Editor). (2) NPC/Mob-Namenssuche fand nur bei exakter Groß-/Kleinschreibung Treffer, weil `locale_name` eine `varbinary`-Spalte ohne Kollation ist (reiner Byte-Vergleich) — behoben über `LOWER(CONVERT(... USING latin1))` auf beiden Seiten, live gegen die echte DB verifiziert (inkl. Umlaute); betrifft auch Shop Editor und Mob Drop Editor, da alle dieselbe `search_items`/`search_mobs`-Funktion nutzen. (3) Neue 6. Vorlage **"Sammel-Quest mit Erfolgschance (Biologie-Stil)"** — Item wird bei jedem Versuch verbraucht, zählt aber nur mit einstellbarer Prozent-Chance zum Fortschritt (Muster 1:1 aus `Biologie/Orkzahn.lua` übernommen). **Noch nicht erneut live getestet** (siehe [[m2manager_quest_builder]])
- [x] **Vier Verbesserungen an bestehenden Editoren** (2026-08-03, aus einer Ideen-Session heraus umgesetzt, siehe `new_features.md` für die Ideensammlung selbst):
  - **Item Editor**: „Als neues Item duplizieren"-Button im Bearbeiten-Modus — holt direkt die nächste freie vnum und wechselt mit den aktuellen Feldwerten vorbefüllt in den Create-Modus (Icon muss weiterhin neu gewählt werden). Vorher musste man dafür Typ/Flags/Werte über „Referenz-Item übernehmen" manuell erneut suchen.
  - **Backup-Browser**: „Diff anzeigen"-Button vor dem Wiederherstellen zeigt einen Zeilen-Diff (via `diff`-npm-Paket) zwischen Backup-Inhalt und aktuellem Stand der Zieldatei, inkl. Fall „Zieldatei existiert aktuell nicht mehr". Backend: `diff_remote_backup`-Command, Zielpfad-Ableitung aus `restore_remote_backup` nach `backups::target_path_for_backup` extrahiert (von beiden geteilt, kann jetzt nicht mehr auseinanderlaufen).
  - **Quest Builder: Volltextsuche** — durchsucht (per Enter/Button) den *Inhalt* aller Quest-Dateien, nicht nur Datei-/Kategorienamen wie die bisherige Live-Filterung. Neue `ssh::read_remote_files`, die eine SFTP-Session für mehrere Dateien wiederverwendet (die bisherigen `read_remote_file`-Aufrufe öffnen pro Datei eine neue SSH-Verbindung — für eine ganze Quest-Kartei viel zu langsam gewesen). Klick auf einen Treffer öffnet die Datei und springt zur Zeile.
  - **Quest Builder: Lua-Syntax-Highlighting** — Rohcode-Editor von einer reinen `<textarea>` auf CodeMirror (`@uiw/react-codemirror` + `@codemirror/legacy-modes`-Lua-Mode) umgestellt, folgt Hell/Dunkel automatisch (MutationObserver auf die `dark`-Klasse an `<html>`, da der Theme-Store das nicht direkt reaktiv exponiert). Cursor-Insert der Funktionsreferenz läuft jetzt über die CodeMirror-`view.dispatch`-API statt DOM-`selectionStart`.
- [x] **Alle 8 Ideen aus `new_features.md` umgesetzt** (2026-08-03):
  - **Mob-Proto-Editor** (`src/features/mob-proto-editor`) und **Account-/Spieler-Verwaltung** (`src/features/account-manager`) — beide auf einer neuen generischen DB-Zeilen-CRUD-Grundlage aufgebaut (`db/explorer.rs::get_row_by_pk`/`update_row`/`insert_row`/`delete_row`, Commands `get_table_row`/`update_table_row`/`insert_table_row`/`delete_table_row`), da `mob_proto`/`account`/`player` anders als `item_proto` nie live gegen einen echten Server verifiziert wurden. Statt Spaltenbedeutung zu raten, zeigt der geteilte `GenericRowEditor` (`src/features/shared/GenericRowEditor.tsx`) jede Spalte roh mit typgerechtem Eingabefeld und erkennt die Primärschlüssel-Spalte automatisch über `information_schema` (`is_primary_key`) statt "id"/"vnum" anzunehmen. Binary/varbinary-Spalten werden beim Schreiben symmetrisch zur bestehenden Lesekodierung Windows-1252-kodiert (schließt denselben Bug-Typ wie bei `locale_name` von vornherein aus). Mob-Proto-Editor: nur Bearbeiten, kein Neuanlegen (Mysql2Proto-Äquivalent für mob_proto nicht verifiziert), Hinweis dass Änderungen i.d.R. erst nach Server-Neustart wirken. Account-Manager: Suche über `account.account`/`player.player` (Suchspalte editierbar, Default `login`/`name`), generischer Insert/Delete auf `player.item` für "Item geben/entfernen" (Fenster/Position-Semantik bewusst nicht geraten, Nutzer füllt Spalten selbst). Kein Kanal zu einem laufenden Spielprozess vorhanden - "Teleport" wirkt bei Online-Spielern ggf. erst nach Neuanmeldung.
  - **Reverse-Lookup für Drops** — neuer "Wer droppt…?"-Button im Mob Drop Editor, rein clientseitig (die Datei ist beim Öffnen schon komplett geladen), kein neuer Backend-Command nötig.
  - **Server-Ressourcen-Monitoring** (`src-tauri/src/resources.rs`) — liest `ps -axo pid,pcpu,pmem,rss,comm` über die bestehende SSH-Verbindung (bewusst kein Linux-`/proc`, da dieser Dev-Server FreeBSD ist), gefiltert nach der neuen Einstellung `server_process_names` (Default `game,db`, nie gegen einen echten Server verifiziert welche Prozessnamen dieser Core nutzt). Im Dashboard als Tabelle sichtbar, sobald SSH verbunden ist. **2026-08-03 erweitert:** neue "Server-Übersicht"-Sektion im Dashboard zeigt zusätzlich die konfigurierte SSH-IP, RAM (`sysctl hw.physmem`/`vm.stats.vm.v_free_count`, FreeBSD-Standard-Sysctls, "frei" zählt bewusst nur echte freie Seiten ohne Cache) und Festplattenbelegung (`df -Pk` auf einem konfigurierbaren Pfad, neue Einstellung `server_disk_path`) — ein einziger kombinierter SSH-Befehl (`get_server_overview`) statt zwei getrennter Verbindungen.
  - **Automatisierte Datenbank-Backups** (`src/features/db-backups`, `src-tauri/src/db_backup.rs`) — `mysqldump`/`mysql`-Restore über SSH mit den bereits konfigurierten MySQL-Zugangsdaten (Passwort via `MYSQL_PWD`, nicht als CLI-Arg sichtbar), neue Einstellungen `db_backup_dir`/`db_backup_databases`. Erstellen/Auflisten/Wiederherstellen/endgültiges Löschen, mit deutlicher Warnung vor dem Restore (überschreibt die komplette Live-DB). **Noch nie live gegen einen echten Server getestet** (wie schon beim Mob Drop Editor SFTP).
  - **Discord-/Webhook-Benachrichtigungen** (`src-tauri/src/webhook.rs`, neue Einstellung `webhook_url`) — generischer `notify_webhook_message`-Command, verdrahtet in: fehlgeschlagene DB-Backups (direkt im Rust-Command) und einen neuen `CrashWatch`-Komponente (`src/components/CrashWatch.tsx`, an der App-Wurzel gemountet), die alle 60s die Server-Ressourcen-Prozessliste prüft und bei Übergang "lief" → "läuft nicht mehr" benachrichtigt. Bewusst kein echter Server-seitiger Watchdog - läuft nur solange die App offen ist.
  - **Command Palette (Strg+K)** (`src/components/CommandPalette.tsx`) — springt zu jedem Sidebar-Bereich, Liste geteilt mit der Sidebar über eine neue gemeinsame `NAV_ITEMS`-Quelle in `store/navigation.ts` (vorher zwei getrennte Listen in Sidebar.tsx).
  - **Icon-/Textur-Browser** (`src/features/icon-browser`) — durchsucht rekursiv alle `.tga` unter `pack/icon/icon/item` im Client-Ordner (neue `icons::list_item_icon_files`/`load_icon_file_data_url`, Thumbnails lazy pro sichtbarer Kachel geladen, mit Pagination), sowohl als eigener Sidebar-Bereich als auch als Auswahl-Modal im Item Editor ("Aus Icon-Browser wählen…").
  - Alle neuen Bereiche in `NAV_ITEMS`/`App.tsx`/i18n (de/en) verdrahtet. `new_features.md` als erledigt markiert.
- [x] **Bugfix: Item Editor zeigte beim Bearbeiten leere/falsche Werte** — echter Root-Cause gefunden (nicht geraten): mehrere `item_proto`-Spalten sind auf diesem Server SIGNED (`type`/`subtype`/`weight`/`size`/`magic_pct`/`limittype0-1`/`applytype0-3`/`specular`/`socket_pct` als signed tinyint, `antiflag`/`flag`/`wearflag`/`gold` als signed int), aber die Rust-Structs erwarteten unsigned — sqlx lehnt das beim Lesen ab, und `.unwrap_or_default()` hat den Fehler bisher lautlos verschluckt (0 statt Fehler). Per `information_schema.columns`-Abfrage gegen die echte Dev-DB verifiziert und alle Typen korrigiert. Zusätzlich: `immuneflag` ist gar kein Bitmask-Integer, sondern ein MySQL `SET('PARA','CURSE','STUN','SLEEP','SLOW','POISON','TERROR')` — eigene Konvertierungs-Helfer geschrieben, `IMMUNE_FLAGS` im Frontend auf die echten SET-Namen korrigiert. Permanenter Regressionstest gegen vnum 3219 auf der echten DB grün (siehe [[m2manager_item_editor]])

---

## 🚧 Fehlt noch / offen

### Kurzfristig sinnvoll (nächste Schritte)
- [ ] **Item Editor v2** — Waffen/Rüstungen mit eigenem `.gr2`-Modell-Import (eigene Ausbaustufe, laut Nutzer bewusst nicht in v1)
- [x] **Item Editor: alle Flag-/Typ-Tabellen gegen Server-Quellcode verifiziert** — `type` (35 statt 6 Einträge), `subtype` (jetzt typabhängiges Dropdown über `SUBTYPES_BY_TYPE`, 14 Item-Typen abgedeckt), `flag` (neues Dropdown statt Rohwert), `wearflag`/`antiflag`/`immuneflag`/`applytype0-3`/`limittype0-1` — alles direkt aus `enum`s in `length.h`/`item_length.h` des echten Server-Quellcodes übernommen. Dabei mehrere echte Fehler in den vorherigen Rate-Tabellen gefunden und korrigiert: `WEAR_FLAGS` hatte Schild/Pfeile vertauscht, `ANTI_FLAGS` war für fast jedes Bit falsch (0x0001 war als "kein Krieger" gelabelt, ist aber FEMALE), und die vom Nutzer selbst gegebene applytype-Liste stimmte ab Wert 3 nicht (siehe [[m2manager_item_editor]])
- [x] **Item Editor: Waffen-/Rüstungs-Schadenswerte (value0-5) vollständig verifiziert** — server- (`battle.cpp`: `Item_GetDamage`/`CalcMeleeDamage`) **und** clientseitig (`Client/root/uitooltip.py`: `__AppendAttackPowerInfo` etc.) geprüft, beide stimmen überein: value3/value4 = Schaden min/max, value5 = Zusatz-Angriffskraft (fließt server- und clientseitig ein), value0 = Angriffstempo-Kategorie, value1/value2 = optionaler Magie-Angriff. Rüstung: value0=Magie-Verteidigung, value1=Verteidigung, value5=Zusatz-Verteidigung. Felder haben jetzt echte Labels statt "value0"/"value1", plus Live-Vorschau "so wird es im Client angezeigt" im Item Editor. Balance/Spieldesign (welche Zahl für welches Level "passend" ist) bleibt bewusst offen — dafür weiterhin "Referenz-Item übernehmen" nutzen (siehe [[m2manager_item_editor]])
- [ ] **Item Editor: value0-5 für alle anderen Typen (nicht Waffe/Rüstung)** — bleibt core-spezifisch, Bedeutung hängt von Typ **und** Subtyp ab (in `char_item.cpp` geprüft, z.B. 34 verschiedene USE-Subtypen mit je eigener Bedeutung). Kontext-Hinweis für USE vorhanden, aber nicht vollständig — „Referenz-Item übernehmen" bleibt der zuverlässigste Weg (siehe [[m2manager_item_editor]])
- [ ] **Weitere Sprachen** — README verspricht 13 Sprachen (FR, ES, PT, IT, NL, TR, RU, RO, CZ, HU, GR …), real vorhanden sind nur DE/EN. Entweder README korrigieren oder Sprachdateien nachziehen (separate Translation-Pipeline aus dem Plan ist noch nicht gebaut)
- [x] **Dashboard-Statistiken** — bereits vollständig mit echten Live-Queries umgesetzt (`get_database_stats` in `db/shop.rs`, zählt Accounts/Spieler/Items/Shops/Mobs; `Dashboard.tsx` zeigt sie inkl. Refresh-Button) — beim Gegenprüfen am 2026-07-29 als bereits fertig bestätigt
- [x] **Bulk-Sync der Stack-Größen** über alle Shops — neu gebaut am 2026-07-29: `sync_shop_stack_sizes` (Tauri-Command, `db/shop.rs::sync_stack_sizes`) überschreibt `shop_item.count` entweder für den aktuellen Shop oder global für alle Shops auf den eingestellten Max-Wert. Button „Bestandsware synchronisieren" im Shop Editor neben dem Max-Stepper, mit Bestätigungsdialog (destruktive Bulk-Aktion) und Ergebnis-Anzeige (Anzahl aktualisierter Zeilen)
- [ ] **Automatisierte Tests für Server-Steuerung** — bisher bewusst nur mit harmlosen Commands (`echo`/`sleep`) getestet, echte Start/Stop-Läufe nie automatisiert verifiziert (siehe [[m2manager_server_control]])
- [ ] **Mob Drop Editor: Live-Test gegen echten Server** — Parser/Serializer sind gegen die echte Beispieldatei des Nutzers unit-getestet (Round-Trip), aber SFTP-Lesen/Schreiben/Backup wurde noch nie live gegen den Dev-Server ausgeführt (siehe [[m2manager_mob_drop_editor]])

### Mittelfristig (im Plan vorgesehen, noch nicht begonnen)
- [ ] **Auto-Updates** — `tauri-plugin-updater` ist noch nicht als Dependency vorhanden, keine GitHub-Releases-Pipeline, kein Update-Badge in der UI
- [x] **Keyboard Shortcuts (teilweise)** — Command Palette (Strg+K, siehe oben) springt zu jedem Bereich; darüber hinausgehende Shortcuts (z.B. Speichern-Shortcut in Editoren) noch nicht gebaut
- [ ] **Translation-Pipeline-Tooling** (separates Dev-Tool: zentrale Translation-DB + LibreTranslate) — laut Plan komplett separates Repo/Tool, noch nicht begonnen
- [ ] **Release-/Build-Pipeline** — Windows-Installer/Portable-Exe-Build (`npm run tauri build`) noch nicht als CI/Release-Workflow eingerichtet

### Bewusst zurückgestellt (Architektur-Entscheidung, kein Bug)
- [ ] **Role-Based Access Control** — aktuell exakt 1 lokales Profil, keine Logins (Absicht)
- [ ] **Team Management / Sync** — Research-Thema für später, zentral gehosteter Sync-Service (Supabase/PocketBase/Turso-artig), Nutzer soll explizit **nicht** selbst hosten müssen — noch nicht designt

---

## Empfehlung für die nächste Session

Priorität, wenn du jetzt weitermachen willst:
1. **Live-Test der neuen DB-/SSH-lastigen Features gegen den echten Dev-Server**: Mob-Proto-Editor, Account-/Spieler-Verwaltung und Datenbank-Backups (mysqldump/restore) wurden nur lokal gebaut/getestet, nie live verifiziert — ähnlich wie beim Mob Drop Editor. Insbesondere `server_process_names` (Ressourcen-Monitoring) und die Primärschlüssel-Erkennung für `account`/`player` verdienen einen echten Blick.
2. **Auto-Updates** aufsetzen (`tauri-plugin-updater` + GitHub Releases) — sobald du an einen Punkt kommst, an dem du Builds an andere verteilen willst
3. **README-Sprachliste korrigieren** (schneller Fix) oder echte i18n-Dateien für die fehlenden Sprachen ergänzen

RBAC/Team-Sync bewusst NICHT vorziehen — laut [[m2manager_plan]] ist das explizit ein späteres Research-Thema.
